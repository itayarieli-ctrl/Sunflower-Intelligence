# ביקורת שכבת הנתונים והלוגיקה — Sunflower

קריאה בלבד. שום דבר לא שונה. מקורות: הסכמה החיה (`pg_constraint`,
`pg_indexes`, `pg_stat_user_tables`), נתוני ייצור, וקוד המקור @ `2b4e886`.

---

## שני הממצאים שמשנים את התמונה

### 1. גבוה — מנוע הציונים כבוי, והמערכת לא יודעת את זה

שאילתה על הייצור: מתוך **123 משפיענים, ל-0 יש את כל ששת הציונים**
שהחישוב דורש. `calculateWeightedScore` מחזיר `null` כשחסר ציון אחד,
ו-recalculation מדלג. כלומר:

- `score_weighted_total` לא מחושב לאף אחד
- כל ה-verdicts מגיעים רק ממסלול הדגלים (veto/ANTI_ISRAEL/…), לא מהמשקלים
- דף המשקלים, ה-recalc, והאינדקסים סביבם — מנגנון שלם שרץ על כלום

זה לא באג בקוד — הקוד נכון. זה **פער נתונים** שהופך את ליבת המוצר
לדוממת. ההחלטה: או להשלים ציונים (דרך ה-research pipeline), או להציג
ב-UI "לא מחושב" במפורש כדי שלא ייראה כאילו יש verdict מבוסס-משקלים.

### 2. גבוה — כפילויות בייצור, ואין אילוץ שימנע את הבאות

בייצור עכשיו: `Crohn's & Colitis UK` ×2, `Crohn's & Colitis Foundation` ×2.

אילוצי ייחודיות קיימים על `events(event_id)`, `country_strategies(country_code)`,
`app_settings(key)`, `app_config(category,key)` — **אבל לא על
`influencers.name` ולא על `organizations.name`**. מסלולי הייבוא
(SmartImport, DataImport, seed) יכולים להכפיל ישויות בכל הרצה, וכל
כפילות מזהמת את הניתוחים שנשענים על הישות.

תיקון עתידי (לא עכשיו): ניקוי הכפולות הקיימות ← אילוץ ייחודיות
(או unique index על `lower(name)`), ואז upsert בייבוא.

---

## בינוני — פצצות מושהות (נכונות היום, יישברו בעומס/מקרה קצה)

### 3. `is_latest` — עדכון-ואז-הכנסה בלי טרנזקציה

`SmartImport.tsx:305-315`: קודם `UPDATE is_latest=false` לכל דוחות
הישות, ואז `INSERT` של החדש. אם ה-INSERT נכשל אחרי שה-UPDATE הצליח —
הישות נשארת **בלי דוח latest בכלל**. שני ייבואים מקבילים = מרוץ.
תיקון נכון: פונקציית DB אחת (RPC) שעושה את שניהם בטרנזקציה.

### 4. עדכון batch של משפיענים — לולאה סדרתית לא אטומית

`useVerdictRecalculation.ts:88`: `for … await UPDATE` אחד-אחד. על 123
משפיענים = 123 סבבי רשת; כשל באמצע משאיר חצי מעודכן בלי סימון. ההערה
בקוד מודה בזה. כרגע moot (0 מעודכנים — ממצא 1), אבל ברגע שהציונים
יגיעו זה יופעל. תיקון: RPC אחד עם מערך, או `upsert` מרובה שורות.

### 5. `imported_by: user?.user?.id || null` — עכשיו זה נכשל אחרת

`SmartImport.tsx:328`. אחרי סגירת מדיניות ה-NULL (היום), רשומה שנכתבת
עם `null` היא רשומה **שאיש לא יכול לערוך**. הקוד צריך להיכשל בקול אם
אין משתמש, לא לכתוב יתום.

### 6. מרוצי check-then-write

- `saveScoringWeights`: select ← ואז insert/update, למרות שיש
  `UNIQUE(key)`. שני אדמינים במקביל = unique violation. תיקון: `upsert`.
- `enrichment_count`: נקרא מה-state בקליינט ומועלה ב-1 — עדכון אבוד
  בין שני משתמשים. תיקון: increment בצד DB.

---

## נמוך — לתעד ולתזמן, לא לרוץ לתקן

| # | ממצא | הערה |
|---|---|---|
| 7 | 11 FK בלי אינדקס (`imported_by`, `latest_report_id` ×3, …) | ב-123 שורות — אפס השפעה. יכאיב במחיקות הורה ובצמיחה. אינדקסים הקריטיים לשאילתות הקיימות **כן** קיימים |
| 8 | `select('*')` בכל 8 ה-hooks | על `intelligence_reports` זה יגרור את כל ה-Json הכבד (raw_structured…) לכל שורת רשימה. כרגע 0 שורות — לטפל כשה-pipeline יתמלא |
| 9 | דגל `useDatabase` זומבי ב-4 hooks | שריד. ה-toast "הנתונים מקודדים" מטעה — אין עוד נתונים מקודדים; DB ריק/כשל מציג מערך ריק |
| 10 | `src/data/*.ts` הם קובצי **טיפוסים**, לא נתונים | החשש מ-mock data התבדה. השם `data/` מטעה — עניין קוסמטי |

---

## מה שנבדק ויצא נקי

- **DataImport** — לא N+1: קריאת edge-function אחת עם מערך הישויות
- **משקלים** — קריאה וכתיבה לאותו מקום (`app_settings.score_weights`),
  ולידציה `sum===100` חוסמת שמירה, עריכה מוגבלת לאדמין גם ב-UI וגם ב-RLS
- **Realtime** — שני ה-channels מנוקים ב-unmount
- **כשל DB** — מציג ריק, לא נתונים מזויפים
- **אינדקסים לשאילתות החיות** — `influencers(name/country/category)`,
  `research_jobs(status,created_at)`, `page_views(created_at)` — קיימים

---

## סדר מומלץ להמשך (לאישורך, כלום לא בוצע)

1. **החלטת מוצר על ממצא 1** — להשלים ציונים או לשקף "לא מחושב"
2. ניקוי 2 הכפולות + אילוץ ייחודיות (מיגרציה קטנה, דורשת מיזוג ידני של הכפולות)
3. תיקוני קוד 3+4+5 יחד — הם באותו אזור (SmartImport/recalc), קומיט אחד
4. 6 ואילך — הזדמנותי
