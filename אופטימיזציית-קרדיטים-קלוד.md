# אופטימיזציית עלות — ממצאים מהחשבון בפועל

בדיקה על `gemini-genesis-engine` ועל workspace `Itay's Lovable`.
כל הקריאות היו קריאה-בלבד ולא נצרכו קרדיטים.

---

## ממצא 1 — ה-Knowledge משלם על עצמו בכל הודעה

לפרויקט מוגדר `SOVEREIGN ARCHITECT PROTOCOL (v5.0)` באורך **כ-3,600 תווים**.
Knowledge הוא הוראות קבועות שנטענות לסוכן — כלומר העלות הזו משולמת
**בכל פנייה**, גם על תיקון של שורה אחת.

זה לא מסמך גרוע. להפך — הוא מוקפד. הבעיה היא שהוא **תמיד דלוק**.

## ממצא 2 — הפרוטוקול מכריח סבב תשלום כפול

הפרוטוקול קובע:

> `PHASE 1: THE BLUEPRINT (Chat Mode Only)` — "PAUSE. Do NOT write code yet.
> ... Wait for user approval before generating code."

כלומר כל פיצ׳ר חדש מחייב **סבב chat ב-Lovable לפני סבב ה-build**. בצ׳אט
של Lovable כל הודעה נספרת. התוצאה: משלמים פעמיים על כל פיצ׳ר — פעם על
התכנון ופעם על הביצוע.

והתכנון הזה הוא בדיוק מה שקלוד עושה בחינם.

## ממצא 3 — מנגנון השימוש-החוזר לא מנוצל

`list_workspace_skills` החזיר **0 skills** על 22 פרויקטים. Skills נשמרים
כ-`skills/{name}/SKILL.md` ברפו של ה-workspace, עם frontmatter של `name`
ו-`description` — כלומר מנגנון שנטען לפי הקשר, לא תמיד-דלוק כמו knowledge.
זה בדיוק הכלי שנועד לתוכן כזה, והוא עומד ריק.

## ממצא 4 — GitHub sync לא ניתן להפעלה מכאן

תיעוד הכלי `add_connector` מפורש:

> "Connectors must always be added through the dashboard — the MCP cannot
> add them programmatically."

זו פעולה שחייבת להתבצע בממשק של Lovable. אני יכול רק להפיק את הקישור.

---

## המבנה המוצע: דק תמיד-דלוק, שמן לפי דרישה

```
לפני                          אחרי
─────                         ─────
Knowledge: 3,600 תווים        Knowledge: ~600 תווים
  → נטען בכל הודעה              → נטען בכל הודעה
                              Skill: שאר הפרוטוקול
Phase 1 בצ׳אט Lovable           → נטען לפי הקשר, ומשותף ל-22 פרויקטים
  → קרדיט
                              Phase 1 בקלוד
Phase 2 ב-Agent                 → 0 קרדיטים
  → קרדיט
                              Phase 2 ב-Agent
                                → קרדיט אחד, עם ספק מוגמר
```

**שלוש נקודות החיסכון:** הקטנת המטען הקבוע לכל הודעה, ביטול סבב התכנון
המשולם, והפיכת הפרוטוקול לנכס משותף לכל ה-workspace במקום עותק לפרויקט.

---

## מה שנשאר ב-Knowledge (האינווריאנטות בלבד)

אלה הכללים שחייבים להיות דלוקים תמיד, כי הפרה שלהם היא באג אמיתי:

```
RLS חובה על כל טבלה. ברירת מחדל: deny all, ואז פתיחה ממוקדת לפי auth.uid().
טיפוסים מגונרטים של supabase-js. any אסור.
אין נתוני mock קשיחים לנתונים עסקיים — לזרוע ב-DB.
סודות רק ב-Edge Functions דרך Deno.env.get(). לעולם לא בקליינט.
Mobile first. יעדי מגע 44x44px לפחות.
קובץ מעל 150 שורות — להציע refactor.
כשל פעמיים על אותו באג — לעצור ולחזור לארכיטקטורה, לא ללופ.
```

## מה שעובר ל-Skill (נטען לפי דרישה)

הפרסונות, פירוט שלבי הביצוע, ה-execution prompts לפי תפקיד, וה-pre-flight
checklist. להלן תוכן מוכן ל-`create_workspace_skill`:

```markdown
---
name: sovereign-architect
description: Enterprise-grade execution protocol for Lovable projects — atomic build order, security fortress rules, component architecture standards, and pre-flight verification. Use when implementing features, refactoring, or debugging.
---

# Sovereign Architect — Execution Protocol

## Build order (atomic batches, to limit regression)
1. Backend core — tables, then RLS policies, then edge functions
2. Data layer — generated types, Zod schemas, React Query hooks
3. UI — isolated presentational components first
4. Integration — wire data to UI, then interactivity

## Component architecture
- Single responsibility: split any component doing fetching AND rendering AND complex state
- Composition over prop drilling beyond three levels
- Files over 150 lines: propose a refactor

## Defensive programming
- Validate every input with Zod: edge function params, form data, URL params
- Error boundaries around major feature blocks — never a white screen
- Every mutation shows loading, success, and human-readable error states

## Role-based response
- "slow" → audit re-renders, memoize, virtualize lists, hunt N+1 queries
- "save settings" → dedicated table, 1:1 to auth.users, RLS policies
- "make it professional" → spacing tokens, type scale, WCAG contrast

## Pre-flight checklist
- [ ] Stacks correctly at 320px
- [ ] Data persists to the database, not local state
- [ ] Console clean — zero warnings
- [ ] RLS policies active
```

---

## סדר הפעולות

| # | פעולה | מי מבצע | סטטוס |
|---|---|---|---|
| 1 | GitHub sync על הפרויקט | אתה, בממשק Lovable | חסום ל-MCP |
| 2 | יצירת ה-skill ב-workspace | אני, באישורך | ממתין |
| 3 | קיצור ה-Knowledge | אני, באישורך | ממתין — דורס תוכן קיים |
| 4 | כיבוי Max mode | אתה, בהגדרות | חסום ל-MCP |

פעולה 3 דורסת טקסט שכתבת. לא אבצע אותה בלי אישור מפורש.

## הערת שמות

שם ה-skill בצד של Lovable חייב להיות ASCII ולהתאים ל-frontmatter, ולכן
`sovereign-architect` ולא שם בעברית — שם עברי צפוי לשבור את המנגנון.
