# ביקורת תשתית — Sunflower Strategic BizDev

בוצע על `itayarieli-ctrl/gemini-genesis-engine` @ `ba02943`.
שיטה: שחזור כרונולוגי של 37 המיגרציות — כל `CREATE POLICY` ו-`DROP POLICY`
הופעלו לפי סדר זמנים כדי לקבל את **המצב הסופי החי**, לא את מה שכתוב
בקובץ בודד. אימות אמפירי מול ה-API לא התאפשר (הפרוקסי חוסם את הדומיין),
ולכן הממצאים נשענים על סמנטיקה של Postgres, שהיא חד-משמעית כאן.

---

## התגלית המרכזית: ניקיון אבטחה שלא הושלם

ב-9 במאי 2026 בוצע מעבר אבטחה שסגר גישה אנונימית. שתי מיגרציות
(`20260509154918`, `20260509160830`) הסירו את המדיניות `Anyone can read`
משלוש טבלאות:

```
✅ influencers        "Anyone can read influencers"    — הוסר
✅ organizations      "Anyone can read organizations"  — הוסר
✅ research_prompts   "Anyone can read prompts"        — הוסר
```

**שלוש טבלאות נשארו מאחור.** הכוונה ברורה מהדפוס עצמו — זו לא החלטה
מודעת אלא ניקיון חלקי.

---

## ממצאים לפי סדר חומרה

### 1. גבוה — שלוש טבלאות פתוחות לקריאה אנונימית

| טבלה | מדיניות | תנאי |
|---|---|---|
| `events` | `Anyone can read events` | `USING (true)`, ללא `TO` |
| `country_strategies` | `Anyone can read country_strategies` | `USING (true)`, ללא `TO` |
| `app_config` | `Anyone can read app_config` | `USING (true)`, ללא `TO` |

מדיניות ללא סעיף `TO` חלה ב-Postgres על `PUBLIC` — כולל התפקיד `anon`.
המפתח הפומבי נמצא ב-bundle של הדפדפן מעצם היותו `VITE_*`, ולכן כל אדם
שפותח את האתר יכול לחלץ אותו ולשלוף את שלוש הטבלאות במלואן.

`app_config` מכיל את מדרגות ה-CPM של מחשבון העלויות — כלומר מודל התמחור
חשוף.

**מדוע זה חמור במיוחד:** כל המסלולים באפליקציה עטופים ב-`ProtectedRoute`
עם `requiredRole`. הממשק אוכף הרשאות, אבל שכבת הנתונים לא. זו הגנה
בצד-לקוח בלבד — היא נעקפת בקריאת REST ישירה, בלי לפתוח את האתר.

`app_settings` גם פתוח לקריאה, אבל זה כנראה מכוון — מצב תחזוקה חייב
להיקרא לפני התחברות. לא מסומן כתקלה.

### 2. גבוה — `events` נשארה עם הרשאת עדכון רחבה

אותו ניקיון שדרג את הרשאות העדכון בשלוש טבלאות מ-"כל משתמש מחובר"
ל-`has_role(moderator/admin)`:

| טבלה | UPDATE | תנאי |
|---|---|---|
| `influencers` | `TO authenticated` | `has_role(... 'moderator')` ✅ |
| `organizations` | `TO authenticated` | `has_role(... 'moderator')` ✅ |
| `country_strategies` | `TO authenticated` | `has_role(... 'moderator')` ✅ |
| **`events`** | **ללא `TO`** | **`auth.role() = 'authenticated'`** ❌ |

כל משתמש רשום — כולל כזה שממתין לאישור — יכול לערוך כל אירוע.

### 3. בינוני — חור עדכון ב-`intelligence_reports`

```sql
USING (auth.uid() = imported_by OR imported_by IS NULL)
```

הקוד ב-`SmartImport.tsx:328` כותב:
```ts
imported_by: user?.user?.id || null
```

כלומר כשההקשר של המשתמש לא נטען, הרשומה נשמרת עם `imported_by = NULL` —
ואז **כל משתמש מחובר יכול לערוך אותה**. הצירוף של שני הדברים יוצר את
החור; כל אחד מהם לבדו נראה תמים.

### 4. בינוני — אין מדיניות DELETE על טבלאות הליבה

`influencers`, `organizations`, `events`, `country_strategies`,
`intelligence_reports` — אפס מדיניות DELETE. תחת RLS זה אומר שכל מחיקה
נכשלת בשקט.

כרגע זה לא באג פעיל: הממשק קורא ל-`.delete()` רק ב-`useEntityNotes.ts`
וב-`useUsersManagement.ts`, ולשתי הטבלאות האלה יש כיסוי. אבל ברגע
שתוסיף כפתור מחיקה לישות ליבה — הוא ייכשל בלי הודעת שגיאה.

### 5. נמוך — דפוס `auth.role() = 'authenticated'`

מופיע בכל מדיניות ה-INSERT. Supabase ממליצה על `TO authenticated` במקום.
המדיניות האלה חסרות סעיף `TO`, כלומר הן נבחנות גם עבור `anon` והתנאי הוא
מה שמציל אותן. זה עובד, אבל שביר.

---

## מה שבנוי נכון — לא לגעת

בדקתי במפורש וזה תקין:

- **`user_roles` היא טבלה נפרדת מ-`profiles`.** זה הדפוס הנכון. אילו
  התפקיד היה שדה ב-`profiles`, כל משתמש היה מסלים לעצמו הרשאות דרך
  המדיניות `Users can update their own profile`.
- **`has_role()` היא `SECURITY DEFINER` עם `SET search_path = public`.**
  שני הפרטים נחוצים — בלי `search_path` קבוע הפונקציה חשופה לחטיפה.
- **RLS מופעל על כל 15 הטבלאות**, ולכולן יש מדיניות בפועל.
- **`profiles`** — קריאה ועדכון מוגבלים ל-`auth.uid() = id`, ואדמין
  רואה הכל. תקין.

---

## תיקון מוצע — מיגרציה אחת

לא הורץ. מוגש לאישור.

```sql
-- 1. סגירת הקריאה האנונימית שנשארה מהניקיון של מאי
DROP POLICY IF EXISTS "Anyone can read events" ON public.events;
DROP POLICY IF EXISTS "Anyone can read country_strategies" ON public.country_strategies;
DROP POLICY IF EXISTS "Anyone can read app_config" ON public.app_config;

CREATE POLICY "Authenticated users can read events"
  ON public.events FOR SELECT TO authenticated USING (true);
CREATE POLICY "Authenticated users can read country_strategies"
  ON public.country_strategies FOR SELECT TO authenticated USING (true);
CREATE POLICY "Authenticated users can read app_config"
  ON public.app_config FOR SELECT TO authenticated USING (true);

-- 2. יישור events לרמת ההרשאה של שאר טבלאות הליבה
DROP POLICY IF EXISTS "Authenticated users can update events" ON public.events;
CREATE POLICY "Moderators and admins can update events"
  ON public.events FOR UPDATE TO authenticated
  USING (has_role(auth.uid(),'moderator'::app_role) OR has_role(auth.uid(),'admin'::app_role))
  WITH CHECK (has_role(auth.uid(),'moderator'::app_role) OR has_role(auth.uid(),'admin'::app_role));

-- 3. סגירת חור ה-NULL בדוחות
DROP POLICY IF EXISTS "Authenticated users can update own reports" ON public.intelligence_reports;
CREATE POLICY "Users can update own reports"
  ON public.intelligence_reports FOR UPDATE TO authenticated
  USING (auth.uid() = imported_by)
  WITH CHECK (auth.uid() = imported_by);
```

**לפני הרצה — שתי בדיקות חובה:**

1. **האם `events` או `country_strategies` נקראות במסך שלפני התחברות?**
   אם כן, סגירתן תשבור אותו. לפי מפת המסלולים שתיהן מאחורי
   `ProtectedRoute`, ולכן צפוי שאין בעיה — אבל שווה לוודא.
2. **כמה רשומות ב-`intelligence_reports` הן עם `imported_by IS NULL`?**
   סעיף 3 ינתק את היוצר מהן. אם יש כאלה, צריך למלא אותן קודם.

בנוסף, כדאי לתקן את `SmartImport.tsx:328` כך שייכשל במקום לכתוב `NULL`.
