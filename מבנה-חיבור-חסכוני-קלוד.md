# מבנה חיבור חסכוני בין קלוד ל-Lovable

## הבעיה: שני מונים נפרדים

| מונה | מה שורף אותו | מה לא שורף אותו |
|---|---|---|
| **קרדיטים ב-Lovable** | `send_message`, `create_project` — כל פנייה לסוכן של Lovable | קריאה בלבד, פוש דרך git |
| **טוקנים בקלוד** | תוצאות של כלי MCP שנכנסות לקונטקסט | עבודה מקומית עם grep/sed |

הטעות הנפוצה היא לייעל מונה אחד ולשלם על השני. קריאת קוד דרך MCP לא
עולה קרדיטים — אבל היא יקרה מאוד בטוקנים. פוש דרך git זול בשני המונים.

**הדגמה מדודה:** קריאה בודדת ל-`list_projects` בלי סינון החזירה
152,000 תווים — כ-38 אלף טוקנים בקריאה אחת, על 22 פרויקטים.

---

## המבנה המומלץ: git הוא הצינור, MCP הוא השלט

```
        ┌──────────────────────────────────────────┐
        │  קלוד — כאן קורה כל החשיבה (זול)          │
        │  תכנון · כתיבת קוד · דיבוג · ניסוח ספק    │
        └───────────────┬──────────────────────────┘
                        │
        ┌───────────────▼──────────────┐
   (1)  │  GitHub — הצינור הראשי        │  0 קרדיטים
        │  קלוד עורך קבצים ודוחף        │  טוקנים מינימליים
        └───────────────┬──────────────┘
                        │  sync דו-כיווני
        ┌───────────────▼──────────────┐
   (2)  │  Lovable MCP — קריאה בלבד     │  0 קרדיטים
        │  get_project, get_diff        │  טוקנים: לצמצם בסינון
        └───────────────┬──────────────┘
                        │
        ┌───────────────▼──────────────┐
   (3)  │  send_message — המנוע היקר    │  קרדיטים בכל קריאה
        │  רק למה שרק Lovable יודע      │  לרכז לפנייה אחת
        └──────────────────────────────┘
```

### שכבה 1 — GitHub sync (השינוי הכי משמעותי)

מפעילים בפרויקט: `GitHub → Connect`. מרגע זה קלוד עורך את הקוד ישירות
ברפו ודוחף, ו-Lovable קולט את הקומיט.

למה זה חוסך בשני הכיוונים:
- **קרדיטים:** קרדיטים נצרכים על פניות לסוכן של Lovable. פוש ל-git הוא
  לא פנייה לסוכן, ולכן שינוי קוד דרך git לא נספר.
- **טוקנים:** קריאה מקומית מאפשרת `grep` ממוקד ו-`sed -n '40,80p'`
  במקום לשאוב קבצים שלמים דרך MCP לתוך הקונטקסט.

### שכבה 2 — כלי קריאה ב-MCP

חינם בקרדיטים, אבל נכנסים לקונטקסט. כללים:
- **אף פעם לא `list_projects` בלי `query`.** תמיד לסנן.
- לנעוץ `project_id` בקובץ ברפו — כדי לא לרשום מחדש לעולם.
- `get_diff` עדיף על לולאת `read_file`. אחרי sync, `git diff` עדיף על שניהם.

### שכבה 3 — `send_message`

זו הפנייה היחידה שעולה כסף. לשמור אותה למה שרק Lovable יודע לעשות:
scaffolding של פרויקט חדש, חיווט Lovable Cloud (Supabase, edge functions,
אינטגרציות), ופריסה. שינוי קוד רגיל — לא כאן, אלא דרך git.

---

## שבעה כללים תפעוליים

1. **לתכנן בקלוד, לבצע ב-Lovable.** לנסח ספק מלא ומדויק בקלוד (זול),
   ולשלוח פנייה אחת ל-Lovable במקום חמש איטרציות.
2. **לכבות Max mode לעבודה שוטפת.** הוא פעיל בחשבון שלך והוא מייקר את
   עלות הקרדיטים לפנייה. להדליק רק למשימות שבאמת דורשות אותו.
3. **`set_project_knowledge` פעם אחת לכל פרויקט.** הוראות קבועות שנשמרות
   בצד של Lovable מייתרות חזרה על הקשר בכל הודעה — חיסכון בשתי הרמות.
4. **לא לפצל בקשה לכמה הודעות.** בצ׳אט של Lovable כל הודעה נספרת,
   וב-build mode העלות משתנה לפי מורכבות.
5. **לא לקרוא קוד דרך MCP כשיש git.** זה הבזבזן הגדול של טוקנים.
6. **תיקוני באגים קטנים — בקלוד ישירות.** תיקון טיפוגרפי או שינוי CSS
   דרך `send_message` הוא בזבוז קרדיט מלא.
7. **`plan_mode=true`** אם בכל זאת רוצים לדון מול הסוכן של Lovable —
   אבל עדיף לדון בקלוד ולשלוח החלטה מוגמרת.

---

## טבלת החלטה: איפה לעשות מה

| משימה | איפה | עלות |
|---|---|---|
| שינוי קוד, refactor, תיקון באג | קלוד → git push | ~0 |
| הבנת הקוד, חיפוש, code review | קלוד מקומי (grep) | ~0 |
| פרויקט חדש מאפס | `create_project` | קרדיטים |
| חיווט backend / אינטגרציה | `send_message` | קרדיטים |
| פריסה | `deploy_project` | פרסום ציבורי |
| בדיקת סטטוס פרויקט | `get_project` | טוקנים בלבד |

---

## מה לעשות בפועל, לפי סדר

1. להפעיל GitHub sync על הפרויקט הפעיל. זה נותן את מרבית החיסכון.
2. לכבות Max mode כברירת מחדל.
3. להגדיר `set_project_knowledge` לפרויקט.
4. לנעוץ את ה-`project_id` ברפו.

## מקורות

- https://github.com/lovablelabs/mcp
- https://docs.lovable.dev/introduction/faq
- https://www.nocode.mba/articles/lovable-pricing
