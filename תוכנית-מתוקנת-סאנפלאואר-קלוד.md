# תוכנית מתוקנת — אחרי תקיפה עצמית

מסמך זה **מחליף** את סדר העדיפויות שב-`ביקורת-תשתית-סאנפלאואר-קלוד.md`.
הממצאים שם נכונים; ההערכות והסדר — לא כולם. כאן מה שהשתנה ולמה.

---

## מה התקיפה שינתה

| הנחה שלי | מה מצאתי | תיקון |
|---|---|---|
| "ליישר `events` לשאר הטבלאות" הוא תיקון טכני | **0 מודרטורים, 1 אדמין, 3 משתמשי `user`.** הצמצום היה הופך 3 אנשים לקוראים-בלבד על אירועים | הפך ל**החלטת מוצר**, לא לתיקון |
| `InfluencerModal` הוא רכיב ליבה עם באג ריצה | מרונדר **רק** מ-`Index.tsx` — שלא מנותב | באג אמיתי ב**קוד מת**. למחוק, לא לתקן |
| Edge functions — חשש ל-seed/toggle ללא הגנה | כל 7 ההרסניות **מוגנות באדמין** (JWT → `user_roles` → 403). ה-webhooks משווים סוד ודוחים | **נסגר.** בנוי נכון |
| `page_views` — "נמוך" | 2,366 שורות, שני נתיבי הכנסה אנונימיים, קריאת geo חיצונית לכל hit | **בינוני** |
| RLS הוא שכבת התשתית הראשונה לבדוק | לא הרצתי build לפני שדיברתי על RLS | **הבסיס קודם** — ראה למטה |

---

## ממצאים חדשים מהתקיפה

### A. תשתית — `package-lock.json` מפגר אחרי `package.json`

```
Invalid: lock file's @lovable.dev/cloud-auth-js@0.0.2 does not satisfy ^1.2.1
Missing: @lovable.dev/mcp-js@1.0.0
Missing: @modelcontextprotocol/sdk@1.28.0
Missing: esbuild@0.27.7
```

`npm ci` **נכשל** על כל checkout נקי. Lovable בונה עם `bun.lockb` ולכן לא
מרגישה. אבל כל CI, כל מפתח עם npm, וכל סביבת ענן — שבורים.
**זה הפער התשתיתי הראשון לסגור**, כי הוא חוסם כל בדיקה אוטומטית אחרת.

### B. בסיס — מצב אמיתי אחרי התקנה נקייה

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | **עובר, 0 שגיאות** |
| `vite build` | **עובר**, 18 שניות. שני chunks מעל 500KB |
| `eslint` | **נכשל** — 80 שגיאות, 19 אזהרות |

מתוך 80: **61 הן `no-explicit-any`** — הפרה ישירה של הכלל "any is strictly
banned" מהפרוטוקול שלך עצמך. 20 מהן ב-Edge Functions. 11 ניתנות ל-`--fix`.

### C. קוד מת — 2 קבצים, ~1,100 שורות

- `src/pages/Index.tsx` — לא מנותב ב-`App.tsx`
- `src/components/dashboard/InfluencerModal.tsx` (886 שורות) — מיובא רק מ-Index

שניהם מכילים באגים (`rules-of-hooks` ×2 במודל). למחוק, לא לתקן.

### D. `has_role()` חשוף כ-RPC ל-anon

```
has_role_exec: anon → EXECUTE
```

כל אחד יכול לקרוא `POST /rest/v1/rpc/has_role` ולשאול "האם UUID X הוא
אדמין?". אורקל לחקירת תפקידים. נמוך (דורש UUID), אבל חינם לסגור.

### E. `page_views` — שני נתיבי הכנסה אנונימיים

1. מדיניות RLS ישירה: `Anyone can insert page views` `WITH CHECK (true)`
2. `track-visit` עם `service_role`, ללא rate-limit, עם קריאת geo ל-`ipwhois.app`
   (מכסה חינמית 10k/חודש) **לכל hit**

הקליינט קורא ל-`from('page_views')` ישירות: **4 פעמים.**

---

## מה שנבדק ונמצא תקין (הפסק לדאוג לזה)

- `mcp` edge function — **read-only**, מעביר את ה-JWT של המשתמש, "respects
  the signed-in user's RLS permissions". `verify_jwt=true`. תקין
- `notify-admin-new-user`, `receive-research-results` — `providedSecret !==
  expectedSecret → 401`, **וגם** דוחים הכול אם הסוד לא מוגדר. מצוין
- `analyze-entity` — מאחורי auth, עם rate-limiting
- `ProtectedRoute` — חוסם רינדור עד `authLoading && roleLoading`. **אין מרוץ**
- `useMaintenanceMode` → `app_settings` (לא נסגר). דף תחזוקה בטוח

---

## סדר הביצוע המתוקן

```
1. lockfile      ← חוסם כל אוטומציה. תיקון: npm install + commit lock
2. מיגרציה חלק 1 ← בטוח, ללא החלטות (ראה למטה)
3. החלטה: events ← שלך. ראה שתי האופציות
4. החלטה: page_views
5. קוד מת        ← מחיקת Index.tsx + InfluencerModal
6. lint          ← --fix ל-11, ואז 61 any ידנית (עבודה, לא דחוף)
```

---

## מיגרציה — חלק 1: בטוח, מוכן להרצה

שלושת המקטעים האלה **לא משנים התנהגות לאף משתמש קיים**:

```sql
-- 1a. סגירת הקריאה האנונימית שנשארה מהניקיון של מאי
DROP POLICY IF EXISTS "Anyone can read events" ON public.events;
DROP POLICY IF EXISTS "Anyone can read country_strategies" ON public.country_strategies;
DROP POLICY IF EXISTS "Anyone can read app_config" ON public.app_config;
CREATE POLICY "Authenticated users can read events"
  ON public.events FOR SELECT TO authenticated USING (true);
CREATE POLICY "Authenticated users can read country_strategies"
  ON public.country_strategies FOR SELECT TO authenticated USING (true);
CREATE POLICY "Authenticated users can read app_config"
  ON public.app_config FOR SELECT TO authenticated USING (true);

-- 1b. סגירת חור ה-NULL (0 שורות מושפעות — אומת)
DROP POLICY IF EXISTS "Authenticated users can update own reports" ON public.intelligence_reports;
CREATE POLICY "Users can update own reports"
  ON public.intelligence_reports FOR UPDATE TO authenticated
  USING (auth.uid() = imported_by) WITH CHECK (auth.uid() = imported_by);

-- 1c. סגירת אורקל התפקידים. authenticated נשאר — המדיניות צריכה אותו
REVOKE EXECUTE ON FUNCTION public.has_role(uuid, app_role) FROM anon, PUBLIC;
```

## מיגרציה — חלק 2: `events` UPDATE — **החלטה שלך**

**המצב:** 4 משתמשים. 1 אדמין (שהוא גם `user`), 3 `user` בלבד, 0 מודרטורים.
מאז מאי, ה-3 **כבר לא יכולים** לערוך `influencers`/`organizations`.
על `events` הם **עדיין יכולים**.

| אופציה | SQL | משמעות |
|---|---|---|
| **A — עקביות** | `UPDATE TO authenticated USING has_role(moderator OR admin)` | 3 המשתמשים הופכים לקוראים-בלבד גם על אירועים. תואם את הכוונה מהניקיון של מאי |
| **B — שימור** | `UPDATE TO authenticated USING (true)` | 3 המשתמשים ממשיכים לערוך אירועים. רק מתקנים את הניב `auth.role()` → `TO authenticated` |

אין לי דרך לדעת מה נכון. זו שאלה של מי אמור לערוך אירועים.

## מיגרציה — חלק 3: `page_views` — **החלטה שלך**

אם הקליינט **לא** מכניס ישירות (4 קריאות בקוד) — המדיניות
`Anyone can insert page views` מיותרת: `track-visit` עובד עם `service_role`
ולא צריך אותה. למחוק אותה סוגר נתיב אחד. הנתיב השני (rate-limit ב-`track-visit`)
הוא שינוי קוד, לא מיגרציה.

---

## מה אני מבקש

1. אישור להריץ **חלק 1** (בטוח)
2. **A או B** לחלק 2
3. אישור למחיקת מדיניות `page_views` (חלק 3)
4. אישור לתקן את ה-lockfile ולדחוף ל-`gemini-genesis-engine`
