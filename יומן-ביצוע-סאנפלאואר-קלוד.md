# יומן ביצוע — Sunflower Strategic BizDev

מה חל בפועל, איך אומת, ומה נשאר פתוח. משלים את
`תוכנית-מתוקנת-סאנפלאואר-קלוד.md`.

---

## חל על מסד הייצור (`jtofvaetlpfmlzgiqwzj`)

### 1a — סגירת קריאה אנונימית · חל ✅

```
DROP  "Anyone can read events"              ON events
DROP  "Anyone can read country_strategies"  ON country_strategies
DROP  "Anyone can read app_config"          ON app_config
CREATE "Authenticated users can read {events, country_strategies, app_config}"
       FOR SELECT TO authenticated USING (true)
```

### 1b — סגירת חור ה-NULL · חל ✅

```
DROP   "Authenticated users can update own reports"   ON intelligence_reports
CREATE "Users can update own reports"
       FOR UPDATE TO authenticated
       USING (auth.uid() = imported_by) WITH CHECK (auth.uid() = imported_by)
```

0 שורות בטבלה — אף רשומה לא הושפעה.

### 1c — סגירת אורקל התפקידים · חל ✅

```
REVOKE EXECUTE ON FUNCTION has_role(uuid, app_role) FROM anon, PUBLIC
```

הרשאות אחרי: `authenticated`, `postgres`, `service_role` — EXECUTE.

**נעצר לפני הרצה** מחשש שה-revoke ישבור את בדיקת מצב התחזוקה
(anon קורא `app_settings`, שיש עליו גם מדיניות `has_role`). הוכח בטוח דרך
`EXPLAIN` כ-anon: `Seq Scan` **ללא Filter** — Postgres קיפל
`true OR has_role(...)` ל-`true` והעלים את הקריאה לפונקציה. רק אז הורץ.

---

## אימות אחרי ביצוע — כ-`anon`, בתוך `ROLLBACK`

| בדיקה | תוצאה | משמעות |
|---|---|---|
| `SELECT count(*) FROM app_settings` | **1** | נתיב התחזוקה עובד |
| `SELECT count(*) FROM events` | 0 | סגור |
| `SELECT count(*) FROM country_strategies` | 0 | סגור |
| `SELECT count(*) FROM influencers` | 0 | היה סגור, נשאר |
| `SELECT count(*) FROM app_config` | `42501 permission denied for function has_role` | נדחה |

השורה האחרונה היא **שינוי התנהגות מכוון**: anon על `app_config` מקבל
שגיאת הרשאה במקום תוצאה ריקה. הקליינט לעולם לא מבצע את הקריאה הזו
כ-anon (הצרכן היחיד, `costCalculator`, יושב מאחורי `ProtectedRoute`).
קריאת REST ישירה מקבלת 403 במקום 200 עם הנתונים.

מצב הרשאות סופי על `pg_policies`:

```
app_config            SELECT  {authenticated}
country_strategies    SELECT  {authenticated}
events                SELECT  {authenticated}
events                UPDATE  {public}   auth.role()='authenticated'   ← לא נגעתי
intelligence_reports  UPDATE  {authenticated}   auth.uid() = imported_by
```

---

## חל על הרפו (`gemini-genesis-engine`)

### `package-lock.json` · דחוף ✅ · `ba02943 → 4557b66` על `main`

הוכחה לפני דחיפה:

```
npm install         → רק package-lock.json השתנה (git status)
rm -rf node_modules
npm ci              → exit 0, 510 חבילות
tsc --noEmit        → exit 0
```

גרסאות שנפתרו: `cloud-auth-js 1.2.1` · `mcp-js 1.0.0` · `mcp sdk 1.28.0`.
`bun.lockb` לא נגע — Lovable ממשיכה לבנות איתו.

---

## החלטות שהתקבלו (לא שינויים)

| נושא | החלטה | סיבה |
|---|---|---|
| `events` UPDATE | **נשאר פתוח לכל משתמש מחובר** | "לא יודע — תשאיר". 0 מודרטורים, 3 משתמשי `user` |
| `page_views` anon INSERT | **נשאר** | הקליינט מכניס ישירות ב-4 מקומות — מחיקה תשבור מעקב |

---

## פתוח — דורש החלטה

### מסד הנתונים מקדים את המיגרציות

1a/1b/1c הורצו ישירות. **אין קובץ ב-`supabase/migrations/` שמתאר אותם.**
המשמעות: כל DB שייבנה מחדש מהמיגרציות (branch preview, שחזור, סביבה
חדשה) **יחזור למצב הפתוח**. הקוד והמסד לא מספרים אותו סיפור.

התיקון: קובץ מיגרציה אידמפוטנטי (`DROP IF EXISTS` לפני כל `CREATE`,
כדי שהרצה חוזרת על ה-DB החי לא תיכשל). לא נכתב — לא אושר במפורש.

### לא בוצע, ממתין

- מחיקת קוד מת: `src/pages/Index.tsx` + `InfluencerModal.tsx` (~1,100 שורות, באג hooks)
- lint: 80 שגיאות, 61 `any`. 11 ניתנות ל-`--fix`
- `track-visit`: rate-limit + מגבלת גודל על שדות הקלט

---

## תוספת — 2026-09-07 11:42 UTC

### קובץ מיגרציה · דחוף ✅ · `2b4e886` על `main`

`supabase/migrations/20260907093000_close_anon_reads_and_role_oracle.sql`

סוגר את הפער "המסד מקדים את המיגרציות" שנרשם למעלה. אידמפוטנטי — כל
`CREATE` מקדים `DROP IF EXISTS`. אומת ע"י הרצת גוף הקובץ מול הייצור
בתוך טרנזקציה לפני הקומיט: 4 מדיניות במקום, 0 הרשאות `anon` על `has_role`.

שם הקובץ ב-ASCII בכוונה — מזהה מיגרציה של Supabase, לא מסמך.

---

## תוספת — מיזוג התאומים ותיל-המעידה (2026-09-07)

### מיזוג + מחיקה + אינדקסים · חל בטרנזקציה אחת ✅

לכל זוג: שדות האוצרות (contacts, email, website, founded_year,
strategic_analysis, member_count) נלקחו מהיתום; tags אוחדו; העברית
וציוני התעדוף של המנצח נשמרו. `"Test intel"` נדרס בניתוח האמיתי.
`region` של `ccuk-uk` תוקן ל-`europe_west` — הערך החוקי בטקסונומיה
(שני התאומים החזיקו ערכים לא-חוקיים). היתומים `ccuk` ו-`ccfa` נמחקו.

אימות: 23 ארגונים (מ-25), אפס שמות כפולים, ההפניה של GBR שלמה,
שני האינדקסים הייחודיים קיימים.

**הערת מקור על member_count:** הבעלים לא יודע את הערך האמיתי. נבחרו
ערכי דור-האוצרות (80,000 UK / 150,000 US) — אותו דור שהביא אנשי קשר
מאומתים — ולא ערכי דור-הבדיקה (500,000 / 5,000,000). בחירת-מקור,
לא עובדה מאומתת. הפיך בעדכון אחד.

### מיגרציה · `supabase/migrations/20260907103000_unique_entity_names.sql`

רק הסכמה (האינדקסים). כירורגיית הנתונים היא חד-פעמית ולא שייכת
למיגרציות — ב-DB שנבנה מחדש האינדקס עצמו הוא שיחסום זריעה כפולה.

### הושאר בחוץ במודע

דיווח שגיאות שורה-שורה ב-`seed-influencers-orgs` (סעיף ג): דורש עריכת
קובץ קיים, בניגוד להעדפת העבודה. התנגשות שם תיכשל היום עם שגיאת
constraint גולמית על כל האצווה — רועש, לא אלגנטי. לביצוע באישור מפורש.

---

## תוספת — מקבץ תיקוני קוד (2026-09-07, המשך)

חמישה קומיטים על `main` של רפו האפליקציה, כל אחד אומת ב-tsc (וב-build
היכן שנגע בקוד אפליקציה) לפני הדחיפה:

| קומיט | תוכן |
|---|---|
| `f1b4d22` | lint --fix מכני (התברר בדיעבד כמיותר — ראה `248815a`) |
| `983e7b9` | SmartImport מסרב לשמור דוח בלי בעלים מאומת — toast "ההתחברות פגה" במקום רשומה יתומה |
| `8c6ff14` | זריעה: upsert שורה-שורה עם איסוף שגיאות פר-שורה (id+שם) במקום כשל אצווה גולמי |
| `3eb671e` | מחיקת הקוד המת: `Index.tsx` + `InfluencerModal.tsx` (1,098 שורות, כולל שתי הפרות hooks). קומיט נפרד — שחזור ב-revert אחד |
| `248815a` | תגלית: `supabase/functions/mcp/index.ts` הוא פלט מג'ונרט של ה-Vite plugin (באנר "AUTO-GENERATED — do not edit"). ה-build דרס את תיקון ה-lint הידני. הקובץ הוחזר למצבו המג'ונרט והוחרג מ-eslint |

Lint baseline: 99 בעיות ← **85** (65 שגיאות — כמעט כולן `no-explicit-any`
בקוד מקור אמיתי, עבודה ידנית להמשך; 20 אזהרות).

לקח מהמקבץ: לפני תיקון lint בקובץ — לבדוק אם הוא מג'ונרט. הבאנר היה שם.

---

## תוספת — שכתוב מסמכי ה-Knowledge (2026-09-07)

**סטטוס: בוצע ואומת בקריאה-חזרה.** המבנה התלת-שכבתי מהמסמך
`מסמך-אופטימיזציה-קלוד.md` הושלם במלואו:

| שכבה | לפני | אחרי |
|---|---|---|
| Workspace Knowledge | ריק | ~1,350 תווים: אינווריאנטות אבטחה/קוד/תהליך, כולל "אין סבב תכנון — בצע ישירות" |
| Project Knowledge | פרוטוקול גנרי, ~3,600 תווים | ~2,300 תווים של **עובדות המערכת** |
| Workspace Skill | `sovereign-architect` (נטען לפי הקשר) | ללא שינוי |

השיפור המהותי הוא לא הקיצור אלא ההחלפה: במקום פרסונות ותהליכים
גנריים, הסוכן של Lovable מקבל עכשיו את מה שנלמד בביקורות —
כללי הזהות (slugs, אינדקס השמות הייחודי והתגובה הנכונה לשגיאה שלו),
מודל ההרשאות בפועל (אנונימי סגור, אין DELETE בכוונה, imported_by חובה),
מצב מנוע הציונים הרדום, הקובץ המג'ונרט שאסור לגעת בו, וחובת המיגרציות.
כל שגיאת-סוכן שתימנע בזכות שורה כזו שווה יותר מהחיסכון בתווים.

הפרוטוקול המקורי גובה מילה-במילה ב-`גיבוי-פרוטוקול-מקורי-קלוד.md`
(`e382045`) לפני הדריסה. שחזור = הדבקה אחת.

**נשאר ידני אצל הבעלים:** כיבוי Max mode (אין לזה API).
