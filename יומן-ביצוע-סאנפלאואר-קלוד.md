# יומן ביצוע — Sunflower Strategic BizDev

מה חל בפועל, איך אומת, ומה נשאר פתוח. משלים את
`תוכנית-מתוקנת-סאנפלאואר-קלוד.md`.

---

## חל על מסד הייצור (`jtofvaetlpfmlzgiqwzj`)

### 1a — סגירת קריאה אנונימית · חל ✅

```
DROP  "Anyone can read events"              ON events
DROP  "Anyone can read country_strategies"  ON country_strategies
DROP  "Anyone can read app_config"          ON app_config
CREATE "Authenticated users can read {events, country_strategies, app_config}"
       FOR SELECT TO authenticated USING (true)
```

### 1b — סגירת חור ה-NULL · חל ✅

```
DROP   "Authenticated users can update own reports"   ON intelligence_reports
CREATE "Users can update own reports"
       FOR UPDATE TO authenticated
       USING (auth.uid() = imported_by) WITH CHECK (auth.uid() = imported_by)
```

0 שורות בטבלה — אף רשומה לא הושפעה.

### 1c — סגירת אורקל התפקידים · חל ✅

```
REVOKE EXECUTE ON FUNCTION has_role(uuid, app_role) FROM anon, PUBLIC
```

הרשאות אחרי: `authenticated`, `postgres`, `service_role` — EXECUTE.

**נעצר לפני הרצה** מחשש שה-revoke ישבור את בדיקת מצב התחזוקה
(anon קורא `app_settings`, שיש עליו גם מדיניות `has_role`). הוכח בטוח דרך
`EXPLAIN` כ-anon: `Seq Scan` **ללא Filter** — Postgres קיפל
`true OR has_role(...)` ל-`true` והעלים את הקריאה לפונקציה. רק אז הורץ.

---

## אימות אחרי ביצוע — כ-`anon`, בתוך `ROLLBACK`

| בדיקה | תוצאה | משמעות |
|---|---|---|
| `SELECT count(*) FROM app_settings` | **1** | נתיב התחזוקה עובד |
| `SELECT count(*) FROM events` | 0 | סגור |
| `SELECT count(*) FROM country_strategies` | 0 | סגור |
| `SELECT count(*) FROM influencers` | 0 | היה סגור, נשאר |
| `SELECT count(*) FROM app_config` | `42501 permission denied for function has_role` | נדחה |

השורה האחרונה היא **שינוי התנהגות מכוון**: anon על `app_config` מקבל
שגיאת הרשאה במקום תוצאה ריקה. הקליינט לעולם לא מבצע את הקריאה הזו
כ-anon (הצרכן היחיד, `costCalculator`, יושב מאחורי `ProtectedRoute`).
קריאת REST ישירה מקבלת 403 במקום 200 עם הנתונים.

מצב הרשאות סופי על `pg_policies`:

```
app_config            SELECT  {authenticated}
country_strategies    SELECT  {authenticated}
events                SELECT  {authenticated}
events                UPDATE  {public}   auth.role()='authenticated'   ← לא נגעתי
intelligence_reports  UPDATE  {authenticated}   auth.uid() = imported_by
```

---

## חל על הרפו (`gemini-genesis-engine`)

### `package-lock.json` · דחוף ✅ · `ba02943 → 4557b66` על `main`

הוכחה לפני דחיפה:

```
npm install         → רק package-lock.json השתנה (git status)
rm -rf node_modules
npm ci              → exit 0, 510 חבילות
tsc --noEmit        → exit 0
```

גרסאות שנפתרו: `cloud-auth-js 1.2.1` · `mcp-js 1.0.0` · `mcp sdk 1.28.0`.
`bun.lockb` לא נגע — Lovable ממשיכה לבנות איתו.

---

## החלטות שהתקבלו (לא שינויים)

| נושא | החלטה | סיבה |
|---|---|---|
| `events` UPDATE | **נשאר פתוח לכל משתמש מחובר** | "לא יודע — תשאיר". 0 מודרטורים, 3 משתמשי `user` |
| `page_views` anon INSERT | **נשאר** | הקליינט מכניס ישירות ב-4 מקומות — מחיקה תשבור מעקב |

---

## פתוח — דורש החלטה

### מסד הנתונים מקדים את המיגרציות

1a/1b/1c הורצו ישירות. **אין קובץ ב-`supabase/migrations/` שמתאר אותם.**
המשמעות: כל DB שייבנה מחדש מהמיגרציות (branch preview, שחזור, סביבה
חדשה) **יחזור למצב הפתוח**. הקוד והמסד לא מספרים אותו סיפור.

התיקון: קובץ מיגרציה אידמפוטנטי (`DROP IF EXISTS` לפני כל `CREATE`,
כדי שהרצה חוזרת על ה-DB החי לא תיכשל). לא נכתב — לא אושר במפורש.

### לא בוצע, ממתין

- מחיקת קוד מת: `src/pages/Index.tsx` + `InfluencerModal.tsx` (~1,100 שורות, באג hooks)
- lint: 80 שגיאות, 61 `any`. 11 ניתנות ל-`--fix`
- `track-visit`: rate-limit + מגבלת גודל על שדות הקלט
