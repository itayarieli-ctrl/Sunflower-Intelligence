# מסמך בקרה — תיקוני תשתית, Sunflower Intelligence

**פרויקט Lovable:** `Sunflower Strategic BizDev projectile analysis` (`gemini-genesis-engine`)
**Project ID:** `d47c4603-563b-4790-a151-b899fd9b4ee5`
**Supabase ref:** `jtofvaetlpfmlzgiqwzj` (Lovable Cloud — מנוהל ע"י Lovable, לא בחשבון Supabase של המשתמש)
**עדכון אחרון:** 2026-09-01 · **גרסה 2** (אחרי אימות מול ה-DB החי)

---

## 1. מטרת המסמך

רפרנס יחיד למעקב אחר האודיט והתיקונים. **כל שינוי בסטטוס נרשם כאן לפני המעבר לצעד הבא.** אין עבודה על פריט שלא בטבלה, ואין מעבר לצעד הבא בלי שער אימות שעבר.

## 2. גישה — מה זמין בפועל

| ערוץ | סטטוס | הערה |
|---|---|---|
| `mcp__Lovable__query_database` | ✅ **זמין** | SQL מלא מול ה-DB החי, כולל DDL. **זהו ערוץ העבודה למסד הנתונים.** |
| `mcp__Lovable__send_message` | ✅ זמין | שינויי קוד ע"י סוכן ה-AI של Lovable. צורך קרדיטים. |
| `mcp__Supabase__*` | ❌ חסום | `get_project` מחזיר `You do not have permission`. הארגון היחיד הנראה: `itay arieli Org` (4 פרויקטים אחרים). |
| כתיבת קבצים ישירה ל-Lovable | ❌ | אין כלי כתיבה ב-MCP של Lovable |
| GitHub | ❌ | `itayarieli-ctrl/Sunflower-Intelligence` מכיל מסמכי עבודה בלבד, לא את קוד האפליקציה |

## 3. כללי עבודה

1. **`supabase/migrations/` אינו מקור אמת.** התגלתה סטייה בין התיקייה ל-DB החי. **כל טענה על מצב ה-DB מאומתת ב-`query_database`, לא מהמיגרציות.**
2. שינויי קוד — צעד אחד בכל פעם, מפרט כירורגי, אימות ב-`get_diff` אחרי כל צעד.
3. כתיבות ל-DB — `SELECT` לבדיקה תמיד קודם. DDL רק אחרי אישור מפורש.
4. גיבוי לפני כל שינוי סכימה.

## 4. מקרא

🔵 מאומת · 🟡 בעבודה · 🟢 הושלם · ⚪ נדחה · ❓ פתוח · ⚠️ **תוקן מגרסה 1**

---

## 5. ⚠️ תיקון מהותי לגרסה 1

גרסה 1 של האודיט התבססה על **קוד ומיגרציות בלבד**. אימות מול ה-DB החי הפך שתי מסקנות ומצא את הבעיה האמיתית:

1. **חומרת SEC-1 הוגזמה.** ה-DB החי כבר מגן על UPDATE ל-`influencers`/`organizations` דרך `has_role(moderator|admin)` — הידוק שלא הופיע במיגרציות שקראתי. הטענה "כל משתמש מאומת יכול לשנות הכול" **שגויה**. מה שנותר פתוח: קריאה והוספה.
2. **הבעיה האמיתית אינה אף אחד מממצאי גרסה 1.** המערכת **מעולם לא עבדה מקצה לקצה.** ראה RUN-1 ו-DATA-1.

---

## 6. טבלת ממצאים

### P0 — חוסם: המערכת אינה מתפקדת

| ID | ממצא | ראיה | סטטוס |
|---|---|---|---|
| **RUN-1** | **צינור המחקר מעולם לא הצליח.** 20 ג'obs, **0 דוחות**. 5 נכשלו עם `n8n webhook 404: "The requested webhook POST spike-gemini is not registered. The workflow must be active..."`, 15 בוטלו ידנית. פעילות אחרונה: **2026-02-13** (לפני ~7 חודשים). ה-workflow ב-n8n אינו פעיל, ו-`N8N_RESEARCH_WEBHOOK_URL` מצביע ל-webhook לא רשום. | `research_jobs` | 🔵 מאומת |
| **DATA-1** | **מערכת הניקוד ריקה לחלוטין בפרודקשן.** 123 משפיענים — מתוכם **0** עם `score_*`, **0** עם `verdict_decision`, **0** עם `priority_score`, **0** עם `ui_data`, **0** עם `verdict`/`geopolitical_gate` JSONB. **0 רשומות ב-`intelligence_reports`.** קיים רק מידע הזרע (123 עם `total_followers`, 25 ארגונים, 19 מדינות). מנוע ה-verdict, המיון ב-MCP, ה-KPI וכל שרשרת הגזירה פועלים על NULL. | שאילתות ספירה | 🔵 מאומת |
| **P-1** | אף נתיב כתיבה פעיל לא כותב `score_*`, `flags_*`, `veto_triggered`, `pro_israel_bonus`, `verdict_decision`. `transformResearchOutputToDBRecord` הוא המודול היחיד שממפה אליהן — ללא קורא. **גם אם RUN-1 ייפתר, הציונים לא יגיעו ל-DB.** | `researchFieldMapping.ts`, `receive-research-results/index.ts` | 🔵 מאומת |

### P1 — פערי אבטחה סמויים

| ID | ממצא | מצב נוכחי | סטטוס |
|---|---|---|---|
| **SEC-1** ⚠️ | **קריאה:** `influencers`/`organizations` — `TO authenticated USING (true)`. **הוספה:** `auth.role()='authenticated'` לתפקיד `public`. כלומר כל משתמש מאומת — גם ללא תפקיד, גם "ממתין לאישור" — יכול **לקרוא את כל המאגר ולהוסיף שורות** דרך REST API. שער האישור חוסם רק ב-UI. **עדכון מוגן כראוי** (moderator/admin). | 4 משתמשים, **0 ללא תפקיד** → לא מנוצל כרגע. סיכון סמוי. | 🔵 מאומת |
| **SEC-2** | `intelligence_reports` UPDATE: `auth.uid()=imported_by OR imported_by IS NULL`. `receive-research-results` יוצר דוחות ללא `imported_by`. | **0 דוחות כרגע** → לא ניתן לניצול. **הופך לפעיל ברגע ש-RUN-1 נפתר.** לתקן לפני. | 🔵 מאומת |
| **DRIFT-1** | ה-DB החי מכיל מדיניות שאינה בתיקיית המיגרציות שנקראה. `supabase/migrations/` אינו מראה נאמן של פרודקשן. | — | 🔵 מאומת |

### P2 — באגי קוד (רלוונטיים רק אחרי שיהיו נתונים)

| ID | ממצא | מיקום | סטטוס |
|---|---|---|---|
| **W-1** ⚠️ | ה-trigger דורס `score_weighted_total` במשקלים קשיחים. **כרגע מוֹעָט:** ב-`app_settings` יש רק `maintenance_mode` — **אין כלל שורת `score_weights`**, ואין נתוני ניקוד. באג סמוי שיתפוס ברגע שיהיו נתונים. | `20260129153137`, `useVerdictRecalculation.ts` | 🔵 מאומת |
| **K-1** | `hasValidatedResearch` בודק שדות ש-`mapDbToInfluencer` לא ממפה → KPI תמיד 0. (כרגע היה 0 ממילא בגלל DATA-1 — שתי סיבות נפרדות.) | `dashboardStats.ts`, `useInfluencers.ts` | 🔵 מאומת |
| **V-1** | וטו לא נאכף כשחסר ציון — `recalculateAll` מדלג לפני בדיקת הווטו. | `useVerdictRecalculation.ts` | 🔵 מאומת |
| **E-1** | `enrichment_count` תמיד 1 — השדה לא ממופה בקריאה. | `useInfluencers.ts` | 🔵 מאומת |
| **G-1** | `countryCode` = 2 אותיות ראשונות של שם המדינה ("Germany"→GE במקום DE). | `useOrganizations.ts` | 🔵 מאומת |
| **D-1** | ברירות מחדל שקטות: `region→'europe_west'`, `type→'patient_advocacy'`, `tier→'emerging'`. | `useOrganizations.ts` | 🔵 מאומת |
| **L-1** | `is_latest` נשבר בכשל — SmartImport מסמן `false` לפני ההוספה. | `SmartImport.tsx` | 🔵 מאומת |
| **J-1** | מחרוזת נכתבת לעמודת JSONB (`updates.verdict`). | `importSchema.ts` | 🔵 מאומת |
| **T-1** | `track-visit` — כתיבת service-role ללא אימות, `user_id` מהלקוח, ללא rate limit. | `track-visit/index.ts` | 🔵 מאומת |
| **TEST-1** | אין תשתית בדיקות כלל. | `package.json` | 🔵 מאומת |

### P3 — חוב טכני

| ID | ממצא | החלטה |
|---|---|---|
| **VOC-1** | 4 אוצרות מילים ל-verdict; ערכים לא-ממופים נזרקים בשקט | לתקן |
| **CONF-1** | `ConfidenceLevel` גדולות/קטנות; `'high'` מקובע ב-callback | לתקן |
| **CFG-1** | `app_settings`/`app_config` קריאים אנונימית | לתקן |
| **UI-1** | `runMigrateOrganizations` no-op שמדווח הצלחה | למחוק |
| **RQ-1** | שכבת נתונים לא-אחידה (React Query מותקן ולא בשימוש ב-hooks המרכזיים) | לתקן |
| **DUP-1** | שתי מערכות ניקוד מקבילות | להחליט |
| **TTL-1** | `research_jobs` ללא ניקוי | לדחות |
| **SEL-1** | `.select('*')` על ~150 עמודות | ⚪ נדחה — אין בעיה מדידה |

---

## 7. תוכנית ביצוע (מסודרת מחדש)

**העיקרון:** אין טעם לכייל מנוע ניקוד שלא זורמים אליו נתונים. קודם להזרים נתונים, אחר כך לכייל.

| צעד | תוכן | שער אימות | סטטוס |
|---|---|---|---|
| **0** | אימות RLS + מצב DB חי | הושלם — הניב RUN-1, DATA-1, תיקון SEC-1 | 🟢 |
| **1** | **RUN-1** — לאבחן את n8n: האם ה-workflow קיים ופעיל? מה ערך `N8N_RESEARCH_WEBHOOK_URL`? האם `N8N_WEBHOOK_SECRET` מוגדר? | ג'וב מחקר יחיד מסתיים ב-`complete` ויוצר `intelligence_report` | ⬜ |
| **2** | **SEC-2** — לתקן לפני שנוצרים דוחות (זול עכשיו, יקר אחר כך) | דוח עם `imported_by` null אינו ניתן לעדכון ע"י משתמש זר | ⬜ |
| **3** | **P-1** — העברת המיפוי ל-`_shared/` וחיבורו ל-`receive-research-results` | ריצת מחקר מזינה `score_*` ו-`flags_*` בפועל | ⬜ |
| **4** | **SEC-1** — הידוק SELECT/INSERT ל-`has_role` | משתמש ללא תפקיד מקבל 403 ב-API ישיר | ⬜ |
| **5** | **W-1** — משקלים דינמיים + יצירת שורת `score_weights` | שינוי משקל → חישוב מחדש → הערך ב-DB זז | ⬜ |
| **6** | **V-1 + K-1 + E-1** — תיקוני לוגיקה ומיפוי | וטו נאכף; KPI > 0; מונה עולה | ⬜ |
| **7** | **TEST-1** — vitest לשרשרת הניקוד | בדיקות תופסות רגרסיה | ⬜ |
| **8** | פירוט `determineVerdict` ל-SQL | verdict מחושב ב-trigger | ⬜ |
| **9** | P3 לפי סדר | — | ⬜ |

---

## 8. החלטת ארכיטקטורה: מקור אמת יחיד

**הוחלט:** מסד הנתונים הוא מקור האמת לשרשרת `6 ציונים → משוקלל → verdict → priority_score`.

**נימוק (5 זוויות + ניסיון הפרכה):** השרשרת היא פונקציה טהורה — פיצול בין TS ל-SQL **גרם** ל-W-1; `list_influencers` ב-MCP ממיין ומעמד ב-DB; קיימים 5+ נתיבי כתיבה ו-trigger הוא המקום היחיד שלא ניתן לעקוף.

**ההתנגדות היחידה ששרדה:** תרחישי "מה אם" דורשים חישוב שלא נשמר. **מענה:** RPC `preview_scores(weights jsonb)` על אותה פונקציית SQL.

`verdictCalculation.ts` נשאר לתצוגה/הסבר בלבד.

---

## 9. יומן שינויים

| תאריך | פעולה | תוצאה |
|---|---|---|
| 2026-09-01 | אודיט קוד ומיגרציות | 20 ממצאים |
| 2026-09-01 | צעד 0 — אימות RLS | הניב SEC-1, SEC-2 |
| 2026-09-01 | **אימות מול DB חי** | **RUN-1 + DATA-1; SEC-1 מתוקן כלפי מטה; DRIFT-1** |
| 2026-09-01 | מסמך גרסה 2 | סדר עדיפויות שונה מהיסוד |

---

## 10. שאלות פתוחות

| # | שאלה | ממתין ל־ |
|---|---|---|
| 1 | **האם ה-workflow ב-n8n קיים בכלל?** מה ערך `N8N_RESEARCH_WEBHOOK_URL`, והאם `N8N_WEBHOOK_SECRET` מוגדר? חוסם את צעד 1. | המשתמש |
| 2 | האם 123 המשפיענים אמורים לעבור מחקר אוטומטי, או שהוזנו ידנית ומספיקים? | החלטת משתמש |
| 3 | הגדרות Auth בפועל (הרשמה פתוחה? אימות מייל?) — לא נגיש ב-SQL | דשבורד Lovable Cloud |
| 4 | האם `data/influencers.ts` הוא זרע היסטורי בלבד (DUP-1) | החלטת משתמש |
