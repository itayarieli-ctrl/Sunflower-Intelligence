# מסמך בקרה — תיקוני תשתית, Sunflower Intelligence

**פרויקט Lovable:** `Sunflower Strategic BizDev projectile analysis` (`gemini-genesis-engine`)
**Project ID:** `d47c4603-563b-4790-a151-b899fd9b4ee5`
**Supabase ref:** `jtofvaetlpfmlzgiqwzj` (Lovable Cloud — מנוהל ע"י Lovable)
**עדכון אחרון:** 2026-09-01 · **גרסה 3** (החלטה: ויתור על n8n)

---

## 1. מטרת המסמך

רפרנס יחיד למעקב אחר האודיט והתיקונים. כל שינוי בסטטוס נרשם כאן לפני המעבר לצעד הבא.

## 2. גישה — מה זמין בפועל

| ערוץ | סטטוס | הערה |
|---|---|---|
| `mcp__Lovable__query_database` | ✅ | SQL מלא מול ה-DB החי כולל DDL. **ערוץ העבודה למסד.** |
| `mcp__Lovable__send_message` | ✅ | שינויי קוד ע"י סוכן Lovable. צורך קרדיטים. |
| `mcp__Supabase__*` | ❌ | `You do not have permission` — הפרויקט תחת הארגון של Lovable |
| כתיבת קבצים ישירה / GitHub | ❌ | אין כלי כתיבה; המאגר ב-GitHub מכיל מסמכים בלבד |

## 3. כללי עבודה

1. **`supabase/migrations/` אינו מקור אמת** — התגלתה סטייה מול ה-DB החי. כל טענה מאומתת ב-`query_database`.
2. שינויי קוד — צעד אחד, מפרט כירורגי, אימות ב-`get_diff`.
3. `SELECT` תמיד לפני DDL. DDL רק אחרי אישור.

## 4. מקרא

🔵 מאומת · 🟡 בעבודה · 🟢 הושלם · ⚪ נדחה · ❓ פתוח · ⚠️ תוקן מגרסה קודמת

---

## 5. החלטת ארכיטקטורה #1 — ויתור על n8n

**רקע:** המשתמש אישר שמעולם לא הוקם n8n בפרודקשן. 20 ג'ובים, 0 דוחות, פעילות אחרונה 2026-02-13.

**הוחלט: להסיר את n8n מהארכיטקטורה.** הוא פותר בעיית תזמור רב-ספקים; מה שחסר הוא **חוזה נתונים מחובר**. קיים כבר צינור ידני מתפקד (`SmartImport`) שרק אינו מחווט לעמודות הניקוד.

**שלב 1 — מחקר עם אדם בלולאה (ללא תשתית חדשה):**
הפעלת פרומפט v3 → כפתור "העתק פרומפט" עם החלפת משתנים → המשתמש מריץ ב-Deep Research חיצוני → מדביק JSON ל-SmartImport → עובר דרך `transformResearchOutputToDBRecord` → העמודות השטוחות מתמלאות.

**שלב 2 — אוטומציה (רק אם הנפח יצדיק):**
edge function אחת מול API מעוגן-רשת, **אותו פרומפט ואותו חוזה פלט**, ברקע דרך `EdgeRuntime.waitUntil()` + `research_jobs` הקיימת. `receive-research-results` ללא שינוי.

**עיקרון מנחה: הנכס הוא החוזה, לא התזמורת.** אם שני השלבים מייצרים `ResearchOutput` ועוברים באותו mapper — אוטומציה עתידית היא החלפת ספק, לא כתיבה מחדש.

**למה לא לקפוץ לאוטומציה:** הפרומפט בן 46K לא תוקף מעולם. להריץ 123 ישויות על תהליך לא-מתוקף = לחזור על הטעות בקנה מידה. לתקף ידנית על 3–5 ואז להחליט.

## 6. החלטת ארכיטקטורה #2 — מקור אמת יחיד לניקוד

**הוחלט:** ה-DB הוא מקור האמת לשרשרת `6 ציונים → משוקלל → verdict → priority_score`.
**נימוק:** פונקציה טהורה שפוצלה בין TS ל-SQL **גרמה** ל-W-1; `list_influencers` ממיין ומעמד ב-DB; 5+ נתיבי כתיבה ו-trigger הוא היחיד שאינו ניתן לעקיפה.
**התנגדות ששרדה:** תרחישי "מה אם". **מענה:** RPC `preview_scores(weights jsonb)` על אותה פונקציית SQL.

---

## 7. טבלת ממצאים

### P0 — חוסם

| ID | ממצא | ראיה | סטטוס |
|---|---|---|---|
| **RUN-1** | צינור המחקר מעולם לא הצליח. 20 ג'ובים → 0 דוחות. 5 כשלו ב-`n8n webhook 404: "spike-gemini is not registered"`, 15 בוטלו. אחרון: 2026-02-13. **הוחלט: להסיר n8n.** | `research_jobs` | 🔵 מאומת |
| **RUN-2** | **פרומפט המחקר האמיתי מושבת.** `Stage0a_v11_4_partnership_research.md` v3, 46,458 תווים, עם scores/flags/relevance/veto — `is_active = false`. כל שלושת הפרומפטים ל-`influencer_research/stage_0` מושבתים. `trigger-research` נופל ל-fallback של שורה אחת. מלכודת: `.maybeSingle()` ייכשל אם יופעל יותר מאחד. | `research_prompts` | 🔵 מאומת |
| **DATA-1** | מערכת הניקוד ריקה: 123 משפיענים, **0** עם `score_*`/`verdict_decision`/`priority_score`/`ui_data`, **0** `intelligence_reports`. קיים רק מידע זרע. | שאילתות ספירה | 🔵 מאומת |
| **P-1** | אף נתיב כתיבה לא כותב `score_*`/`flags_*`/`veto_triggered`/`pro_israel_bonus`/`verdict_decision`. `transformResearchOutputToDBRecord` ללא קורא. | `researchFieldMapping.ts` | 🔵 מאומת |
| **CON-1** | **שלושה חוזי מחקר בלתי תואמים.** (א) **הפרומפט v3** — markdown עם סמני `=== N. SECTION ===`, ללא JSON, ציונים **/10**. (ב) **הפרסר** `detectSourceFormat` — מחפש `## PHASE 1/2` או ```` ```json ````. (ג) **ה-mapper** `ResearchOutput` — JSON camelCase מקונן, ציונים **0–100**. אף אחד לא תואם לאחר. זהו שורש הכשל. | `research_prompts` v3, `schemaMapper.ts`, `researchOutput.ts` | 🔵 מאומת |
| **SCALE-1** | **פער סולם פי 10.** הפרומפט מנקד `/10`; הספים במערכת הם 75/60/45 על סולם 0–100. מועמד 10/10 בכל מימד → משוקלל 10 → verdict `NO`. אילו הצינור עבד, **כל המועמדים היו נפסלים.** | `research_prompts` v3 מול `verdictCalculation.ts` | 🔵 מאומת |
| **FMT-1** | **הפרסר לא מזהה את פורמט הפרומפט.** סמני `===` אינם ברשימת הזיהוי ואין `{` בפלט → סיווג `text_only` → ניתוב ל-`analyze-entity` (Gemini ללא חיפוש רשת). 13 חיפושים גיאופוליטיים מתועדים נמסרים למודל עיוור לפרשנות מחדש. | `schemaMapper.ts:detectSourceFormat` | 🔵 מאומת |

### P0 — הפרות מפרט (מול Claude Project Instructions v5.0, 21/1/2026 — המסמך הקנוני)

| ID | ממצא | מפרט מול מימוש | סטטוס |
|---|---|---|---|
| **SPEC-1** | **"VETO does NOT stop research" — הקוד עושה ההפך.** המפרט: בווטו המערכת משלימה את כל השלבים, מחשבת את כל המימדים ובונה תיק ראיות מלא; רק ה-verdict הופך HARD_NO. בפועל `analyze-entity` מפסיק מיד ומחזיר 3 שדות. תיק הראיות אובד דווקא במקרים שבהם צריך להצדיק פסילה. | `analyze-entity/index.ts` | 🔵 מאומת |
| **SPEC-2** | **רמה גיאופוליטית חסרה.** המפרט מגדיר `humanitarian_concern` (5/50, דגל SENSITIVE) ומורה ש-`unknown` מתמזג ל-`neutral`. הקוד: אין `humanitarian_concern`, ו-`unknown` נפרד. אבחנה מכוונת שהמערכת לא יכולה לייצג. | `researchOutput.ts`, `analyze-entity` | 🔵 מאומת |
| **SPEC-3** | **רובריקת תתי-הרכיבים אינה בסכימה.** המפרט: Relevance 25+25+5+15+30 · Reach B2C 35+15 / B2B 20+15+15 · Brand Safety 50 גיאו + 50 אחר. הסכימה: מספר אטום אחד לכל מימד. הציונים אינם ניתנים לביקורת או לשחזור — פוגע בהצעת הערך "ציונים אובייקטיביים". | סכימת `influencers` | 🔵 מאומת |
| **SPEC-4** | **הסכימה לא מייצגת את הצינור הקנוני.** המפרט: `0A → 0C → 1 → 1C → 2`. ה-DB: `research_stage CHECK IN ('stage_0','stage_1')`, `research_prompts.stage CHECK IN ('stage_0','stage_1','synthesis')`. אין 0C/1C/Stage 2 — והשלב שמחשב ציונים ו-verdict הוא בדיוק Stage 2. | `20260204124714`, `research_jobs` | 🔵 מאומת |
| **W-1** ⚠️ **הועלה** | המפרט קובע `25/20/15/15/15/10` — זהה ל-`DEFAULT_WEIGHTS` בלקוח. ה-trigger (`0.25/0.15/0.20/0.10/0.15/0.15`) הוא **הצד השגוי**. ועקרון הליבה במפרט הוא *"The user sets the weights"* — כלומר זו **הפרה של ההבטחה המרכזית של המוצר**, לא באג סמוי. | `20260129153137` | 🔵 מאומת |

### P1 — אבטחה ואמינות

| ID | ממצא | מצב | סטטוס |
|---|---|---|---|
| **HALLU-1** | `analyze-entity` קורא ל-Gemini **ללא חיפוש רשת**, אך הפרומפט מורה לבצע חיפושים ולהחזיר `evidence` עם `source`+`date`. מודל בלי רשת ימציא ראיות → פסילת אדם אמיתי כ"אנטי-ישראל" על בסיס מקור בדוי. נזק ממשי + חשיפה משפטית. | פעיל | 🔵 מאומת |
| **SEC-1** ⚠️ | קריאה: `TO authenticated USING(true)`. הוספה: `auth.role()='authenticated'`. כל משתמש מאומת — גם ללא תפקיד — קורא את כל המאגר ומוסיף שורות דרך API. **עדכון מוגן כראוי** (moderator/admin). | 4 משתמשים, 0 ללא תפקיד → סמוי | 🔵 מאומת |
| **PII-1** | **חשיפת מידע אישי של אנשים אמיתיים.** סורק האבטחה של Lovable מדווח `influencer_contact_pii` כממצא קריטי פתוח. `contact_emails`, `contact_phones`, `contact_agent` נקראים ע"י כל משתמש מאומת (SEC-1). רבים מהמשפיענים באירופה → רלוונטיות GDPR. זו אינה חשיפת מידע קנייני בלבד אלא **מידע אישי של צדדים שלישיים שלא נתנו הסכמה**. | חשוף | 🔵 מאומת |
| **SEC-2** | `intelligence_reports` UPDATE: `imported_by IS NULL` → כל מאומת. הצינור יוצר דוחות ללא `imported_by`. | 0 דוחות → סמוי. **לתקן לפני שיהיו.** | 🔵 מאומת |
| **DRIFT-1** | ה-DB החי מכיל מדיניות שאינה בתיקיית המיגרציות. | — | 🔵 מאומת |

### P2 — באגי קוד (רלוונטיים משיהיו נתונים)

| ID | ממצא | מיקום | סטטוס |
|---|---|---|---|
| **W-1** ⚠️ | trigger דורס `score_weighted_total` במשקלים קשיחים. כרגע מוֹעָט — אין שורת `score_weights` ואין נתוני ניקוד. סמוי. | `20260129153137` | 🔵 |
| **V-1** | וטו לא נאכף כשחסר ציון. | `useVerdictRecalculation.ts` | 🔵 |
| **K-1** | KPI תמיד 0 — שדות לא ממופים ב-`mapDbToInfluencer`. | `dashboardStats.ts` | 🔵 |
| **E-1** | `enrichment_count` תמיד 1. | `useInfluencers.ts` | 🔵 |
| **G-1** | `countryCode` = 2 אותיות משם המדינה. | `useOrganizations.ts` | 🔵 |
| **D-1** | ברירות מחדל שקטות מזייפות סטטיסטיקה. | `useOrganizations.ts` | 🔵 |
| **L-1** | `is_latest` נשבר בכשל. | `SmartImport.tsx` | 🔵 |
| **J-1** | מחרוזת לעמודת JSONB. | `importSchema.ts` | 🔵 |
| **T-1** | `track-visit` ללא אימות/rate-limit. | `track-visit/index.ts` | 🔵 |
| **TEST-1** | אין בדיקות כלל. | `package.json` | 🔵 |
| **VOC-1** | **שישה אוצרות מילים ל-`verdict`.** קנוני (`researchOutput.ts`): `PRIORITY_PARTNER`/`CLEAR_YES`/`HARD_NO`. מולו: `importSchema` (`'CLEAR YES'`, `'PROCEED WITH CAUTION'` — עם רווחים) · `analyze-entity` (`PRIORITY_YES`/`STRONG_YES`/`YES` — לא קיימים בטיפוס) · `schemaMapper.PRIORITY_MAP` (`VIP`/`High`/`Low`/`SKIP`) · `researchPrompt.ts` (`"PRIORITY YES"`/`"VETO"`). רק פרומפט v3 ב-DB תואם לקנוני. גם `riskLevel` מתפצל: `suspected`/`confirmed` מול `suspected_risk`/`confirmed_risk`. ערכים לא-ממופים נזרקים בשקט. | 5 קבצים | 🔵 |
| **PR-4** | **פרומפט מחקר רביעי שלא היה בתמונה:** `src/data/researchPrompt.ts` — פרומפט אצווה (`generateResearchExport`) שמייצא רשימת מועמדים ומצפה למערך JSON. איכותי, אך ללא 6 מימדי ניקוד וללא מערך `flags` — כלומר גם הוא לא מזין את מנוע הניקוד. | `src/data/researchPrompt.ts` | 🔵 |

### P3 — חוב טכני

`CONF-1` confidence · `CFG-1` app_settings אנונימי · `UI-1` no-op · `RQ-1` React Query · `DUP-1` ניקוד כפול · `TTL-1` ניקוי jobs · `SEL-1` ⚪ נדחה

---

## 8. תוכנית ביצוע

**עיקרון:** קודם להזרים נתונים, אחר כך לכייל.

| צעד | תוכן | שער אימות | סטטוס |
|---|---|---|---|
| **0** | אימות RLS + DB חי | הניב RUN-1/2, DATA-1, CON-1, תיקון SEC-1 | 🟢 |
| **1** | **CON-1 + SCALE-1 + FMT-1 + ENC-1** — להוסיף לפרומפט v3 סקציה `=== 11. STRUCTURED OUTPUT ===` עם בלוק ```json בסכמת `ResearchOutput`, **ולהמיר את כל הפרומפט לסולם 0–100**; לתקן את הקידוד. | הפלט מזוהה `hybrid`: נרטיב ל-`full_dossier`, JSON למיפוי | ⬜ |
| **2** | **P-1** — חיווט `transformResearchOutputToDBRecord` למסלול ה-hybrid ב-SmartImport | הדבקה אחת ממלאת `score_*` ו-`flags_*` | ⬜ |
| **2ב** | **RUN-2** — הפעלת פרומפט v3 (בדיוק אחד פעיל) + כפתור העתקה עם החלפת משתנים | הפרומפט מועתק עם שם הישות מוחלף | ⬜ |
| **3** | **תיקוף** — 3–5 ישויות מקצה לקצה | דירוג ו-verdict אמיתיים בדשבורד | ⬜ |
| **4** | **HALLU-1** — סימון `NOT_VERIFIED` במקום ראיות מומצאות | אין `evidence` ללא מקור בר-אימות | ⬜ |
| **5** | **SEC-2 + SEC-1** — הידוק הרשאות | משתמש ללא תפקיד → 403 | ⬜ |
| **6** | **W-1** — משקלים דינמיים + שורת `score_weights` | שינוי משקל מזיז ערך ב-DB | ⬜ |
| **7** | **V-1 + K-1 + E-1** | וטו נאכף; KPI>0; מונה עולה | ⬜ |
| **8** | **TEST-1** — vitest לשרשרת הניקוד | תופס רגרסיה | ⬜ |
| **9** | `determineVerdict` ל-SQL | verdict ב-trigger | ⬜ |
| **10** | P3 + ניקוי שרידי n8n | — | ⬜ |

---

## 9. יומן שינויים

| תאריך | פעולה | תוצאה |
|---|---|---|
| 2026-09-01 | אודיט קוד ומיגרציות | 20 ממצאים |
| 2026-09-01 | אימות מול DB חי | RUN-1, DATA-1; SEC-1 מתוקן כלפי מטה; DRIFT-1 |
| 2026-09-01 | בדיקת `research_prompts` | **RUN-2** — הפרומפט האמיתי מושבת |
| 2026-09-01 | אישור המשתמש: n8n מעולם לא הוקם | **החלטה: ויתור על n8n**; CON-1 הוא החסם האמיתי |
| 2026-09-01 | ניתוח תוכן פרומפט v3 (46K) | **SCALE-1** (פער ×10) + **FMT-1** (פרסר עיוור לפורמט) + **ENC-1**. הפרומפט בשל ואיכותי — הפער הוא בחיווט, לא בתוכן. חיפוש ב-Google Drive: 12 קבצים, אף אחד רלוונטי. אין גישה לשיחות/פרויקטים ב-Claude.ai. |

---

## 10. שאלות פתוחות

| # | שאלה | ממתין ל־ |
|---|---|---|
| 1 | באיזה כלי Deep Research תשתמש בשלב 1? (משפיע על פורמט ההוראות בפרומפט) | המשתמש |
| 2 | לתקף על אילו 3–5 ישויות? | המשתמש |
| 3 | הגדרות Auth (הרשמה פתוחה? אימות מייל?) — לא נגיש ב-SQL | דשבורד Lovable Cloud |
| 4 | `data/influencers.ts` — זרע היסטורי בלבד? (DUP-1) | המשתמש |
