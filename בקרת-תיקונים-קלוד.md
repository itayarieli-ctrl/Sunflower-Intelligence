# מסמך בקרה — תיקוני תשתית, Sunflower Intelligence

**פרויקט Lovable:** `Sunflower Strategic BizDev projectile analysis` (`gemini-genesis-engine`)
**Project ID:** `d47c4603-563b-4790-a151-b899fd9b4ee5`
**Supabase ref:** `jtofvaetlpfmlzgiqwzj`
**עדכון אחרון:** 2026-09-01

---

## 1. מטרת המסמך

רפרנס יחיד למעקב אחר האודיט התשתיתי והתיקונים. **כל שינוי בסטטוס נרשם כאן לפני שממשיכים לצעד הבא.** המסמך נועד למנוע סחף: אין עבודה על פריט שלא מופיע בטבלה, ואין מעבר לצעד הבא בלי שער אימות שעבר.

## 2. כללי עבודה

1. **מסלול יישום יחיד:** אין גישת כתיבה ישירה. כל שינוי מבוצע ע"י סוכן ה-AI של Lovable דרך `send_message`, ומאומת אח"כ ב-`get_diff` / `read_file`.
   - ❌ כתיבת קבצים ישירה — ל-MCP של Lovable אין כלי כתיבה
   - ❌ מיגרציות ישירות — ה-DB `jtofvaetlpfmlzgiqwzj` אינו בחשבון ה-Supabase המחובר
   - ❌ GitHub — המאגר `itayarieli-ctrl/Sunflower-Intelligence` מכיל מסמכי עבודה בלבד, לא את קוד האפליקציה
2. **צעד אחד בכל פעם.** מפרט כירורגי, לא בקשה כללית.
3. **שער אימות אחרי כל צעד.** לא ממשיכים בלי לאמת שהסוכן עשה בדיוק את הנדרש.
4. **כל `send_message` צורך קרדיטים של המשתמש.** לא מריצים ניסויים.
5. **גיבוי לפני נגיעה ב-DB.** snapshot ב-Supabase או commit ידוע ב-Lovable לחזרה.

## 3. מקרא סטטוס

| סימון | משמעות |
|---|---|
| 🔵 מאומת | הממצא אושר בקוד, טרם תוקן |
| 🟡 בעבודה | מפרט נשלח לסוכן, ממתין לאימות |
| 🟢 הושלם | תוקן ואומת |
| ⚪ נדחה | הוחלט לא לטפל (עם נימוק) |
| ❓ פתוח | דורש אימות נוסף |

---

## 4. טבלת ממצאים

### P0 — שובר פונקציית ליבה / אבטחה

| ID | ממצא | מיקום | סטטוס |
|---|---|---|---|
| **SEC-1** | **שער האישור הוא ויזואלי בלבד.** כל טבלאות המידע מעניקות קריאה **וכתיבה** לכל JWT מסוג `authenticated`, ללא בדיקת `has_role`. משתמש "ממתין לאישור" נחסם ב-UI ע"י `ProtectedRoute`, אך יכול לקרוא ולשנות את כל המאגר דרך REST API. מחמיר: `enable_signup = true` ו-`enable_confirmations = false` — הרשמה פתוחה ללא אימות מייל. | `20251229171848`, `20260108132231`, `20260509*`, `supabase/config.toml` | 🔵 מאומת |
| **SEC-2** | `intelligence_reports` UPDATE policy: `auth.uid() = imported_by OR imported_by IS NULL`. `receive-research-results` יוצר דוחות **ללא** `imported_by` → כל הדוחות האוטומטיים ניתנים לעריכה ע"י כל משתמש מאומת. | `20260108132231` | 🔵 מאומת |
| **W-1** | **משקלים מותאמים לא נשמרים.** `trigger_update_calculated_scores` נורה על עדכון `verdict_decision` ודורס את `score_weighted_total` במשקלים קשיחים (`0.25/0.15/0.20/0.10/0.15/0.15`), בעוד הלקוח מחשב לפי `app_settings.score_weights` (`25/20/15/15/15/10`). מסך ההגדרות לא משפיע על הציון השמור. | `20260129153137`, `useVerdictRecalculation.ts` | 🔵 מאומת |
| **P-1** | **צינור המחקר לא מזין את מערכת הניקוד.** אף נתיב כתיבה פעיל לא כותב `score_*`, `flags_*`, `veto_triggered`, `pro_israel_bonus`, `verdict_decision`. `transformResearchOutputToDBRecord` הוא המודול היחיד שממפה אליהן — ללא קורא. | `researchFieldMapping.ts`, `receive-research-results/index.ts`, `useInfluencers.ts` | 🔵 מאומת |
| **K-1** | **KPI "פרופילים מלאים" תמיד 0.** `hasValidatedResearch` בודק `narrative_*`, `summary_who_is_this_person`, `contact_emails`, `contact_phones` — ו-`mapDbToInfluencer` לא ממפה אף אחד מהם. | `dashboardStats.ts`, `useInfluencers.ts` | 🔵 מאומת |

### P1 — טעויות אמיתיות, נזק מוגבל

| ID | ממצא | מיקום | סטטוס |
|---|---|---|---|
| **V-1** | וטו לא נאכף כשחסר ציון — `recalculateAll` מדלג על ישויות שנכשלות ב-`hasAllScores`, אך בדיקת הווטו קודמת לציונים ואינה תלויה בהם. | `useVerdictRecalculation.ts` | 🔵 מאומת |
| **G-1** | `countryCode` = 2 האותיות הראשונות של **שם** המדינה. "Germany"→`GE` (במקום DE), "United States"→`UN` (במקום US). שובר התאמות בעמוד הגאוגרפיה. | `useOrganizations.ts` | 🔵 מאומת |
| **E-1** | `enrichment_count` תמיד נכתב כ-1 — `mapDbToInfluencer` לא ממפה את השדה, לכן המונה הנוכחי תמיד 0. | `useInfluencers.ts` | 🔵 מאומת |
| **D-1** | ברירות מחדל שקטות מזייפות סטטיסטיקה: `region → 'europe_west'`, `type → 'patient_advocacy'`, `tier → 'emerging'`. | `useOrganizations.ts` | 🔵 מאומת |
| **T-1** | `track-visit` כותב עם service-role ללא אימות, כולל `user_id` מהלקוח, ללא הגבלת קצב. | `track-visit/index.ts` | 🔵 מאומת |
| **L-1** | `is_latest` נשבר בכשל: SmartImport מסמן הכול `false` ואז מוסיף. אם ההוספה נכשלת — אין דוח עדכני. `receive-research-results` עושה זאת בסדר הנכון. | `SmartImport.tsx` | 🔵 מאומת |
| **J-1** | מחרוזת נכתבת לעמודת JSONB: `updates.verdict = data.verdict` (מחרוזת `'CLEAR YES'`) נכתב לעמודת `verdict` שהיא JSONB. קריאת `verdict.decision` תחזיר `undefined`. | `importSchema.ts` | 🔵 מאומת |
| **TEST-1** | אין תשתית בדיקות כלל. ליבת המוצר היא פונקציה טהורה — שלושת באגי ה-P0 היו נתפסים ב-3 בדיקות יחידה. | `package.json` | 🔵 מאומת |

### P2 — חוב טכני

| ID | ממצא | החלטה | סטטוס |
|---|---|---|---|
| **VOC-1** | 4 אוצרות מילים ל-verdict (`CLEAR_YES` / `'CLEAR YES'` / `PRIORITY_YES` / `VIP`). ערכים לא-ממופים נזרקים בשקט. | לתקן | 🔵 מאומת |
| **CONF-1** | `ConfidenceLevel` גדולות מול קטנות; `receive-research-results` מקבע `'high'` לכל דוח. | לתקן | 🔵 מאומת |
| **DUP-1** | שתי מערכות ניקוד מקבילות (`useWeightedScoring` + `data/influencers.ts` מול 6-dim). | להחליט | 🔵 מאומת |
| **UI-1** | `runMigrateOrganizations` הוא no-op שמדווח הצלחה. | למחוק | 🔵 מאומת |
| **CFG-1** | `app_settings` / `app_config` קריאים אנונימית (`USING (true)` ללא `TO authenticated`). | לתקן | 🔵 מאומת |
| **RQ-1** | שכבת נתונים לא-אחידה: `useInfluencers`/`useOrganizations` ידניים בעוד React Query כבר מותקן. | לתקן | 🔵 מאומת |
| **SEL-1** | `.select('*')` על ~150 עמודות ללא pagination. | ⚪ נדחה — אין בעיית ביצועים מדידה בהיקף הנוכחי | ⚪ |
| **TTL-1** | `research_jobs` ללא ניקוי/TTL. | לדחות | 🔵 מאומת |

---

## 5. תוכנית ביצוע

| צעד | תוכן | שער אימות | סטטוס |
|---|---|---|---|
| **0** | אימות RLS לכתיבה | הושלם — הניב SEC-1, SEC-2 | 🟢 הושלם |
| **1** | **SEC-1 + SEC-2** — הידוק RLS ל-`has_role`, סגירת `imported_by IS NULL`, בדיקת הגדרות הרשמה | משתמש ללא תפקיד מקבל 403 בקריאה ישירה ל-API | ⬜ |
| **2** | **W-1** — `get_score_weights()` + `calculate_weighted_score(weights jsonb)` | שינוי משקל בהגדרות → חישוב מחדש → הערך ב-DB זז | ⬜ |
| **3** | **K-1 + E-1** — הוספת שדות חסרים ל-`mapDbToInfluencer` | KPI מציג ערך > 0; `enrichment_count` עולה | ⬜ |
| **4** | **V-1** — הפרדת מסלול הווטו + `CHECK` constraint | ישות עם וטו וציון חסר מקבלת `HARD_NO` | ⬜ |
| **5** | **P-1** — העברת המיפוי ל-`_shared/` וחיבורו ל-`receive-research-results` | ריצת מחקר מזינה `score_*` ו-`flags_*` | ⬜ |
| **6** | **TEST-1** — vitest + בדיקות לשרשרת הניקוד | הבדיקות עוברות ותופסות רגרסיה | ⬜ |
| **7** | פירוט `determineVerdict` ל-SQL (שלב 2 של מקור-אמת אחד) | verdict מחושב ב-trigger; TS לתצוגה בלבד | ⬜ |
| **8** | P2 לפי סדר: VOC-1, CONF-1, CFG-1, UI-1, RQ-1, DUP-1 | — | ⬜ |

**סדר הצעדים אינו שרירותי:** צעד 1 לפני הכול (חשיפת מידע פעילה). צעדים 3-4 חסינים ולא נוגעים ב-DB. צעד 7 אחרון — הוא הגדול, ורק אחרי שהמערכת יציבה ומכוסה בבדיקות.

---

## 6. החלטת ארכיטקטורה: מקור אמת יחיד

**הוחלט:** מסד הנתונים הוא מקור האמת לשרשרת `6 ציונים → משוקלל → verdict → priority_score`.

**נימוק (אחרי בחינה מ-5 זוויות וניסיון הפרכה):**
- השרשרת היא פונקציה טהורה — פיצול בין TS ל-SQL **גרם** ל-W-1 ויגרום לו שוב
- `list_influencers` ב-MCP ממיין ומעמד ב-DB — הערך חייב להיות ידוע ל-Postgres
- קיימים 5+ נתיבי כתיבה; trigger הוא המקום היחיד שלא ניתן לעקוף

**ההתנגדות היחידה ששרדה בדיקה:** תרחישי "מה אם" עם משקלים חלופיים דורשים חישוב שלא נשמר.
**מענה:** RPC `preview_scores(weights jsonb)` שמריצה את אותה פונקציית SQL בלי לכתוב. הכפילות נמנעת.

`verdictCalculation.ts` נשאר **לתצוגה/הסבר בלבד**, מסומן במפורש שאסור לו לכתוב.

---

## 7. יומן שינויים

| תאריך | פעולה | תוצאה |
|---|---|---|
| 2026-09-01 | אודיט תשתיתי מלא | 20 ממצאים מאומתים |
| 2026-09-01 | צעד 0 — אימות RLS לכתיבה | נסגר; הניב SEC-1 + SEC-2 |
| 2026-09-01 | יצירת מסמך בקרה | — |

---

## 8. שאלות פתוחות

| # | שאלה | ממתין ל־ |
|---|---|---|
| 1 | הגדרות ה-Auth בפועל — `config.toml` מוסמך ל-dev מקומי בלבד. יש לאמת בדשבורד Supabase אם ההרשמה פתוחה ואם אימות מייל כבוי. | בדיקה בדשבורד |
| 2 | האם `data/influencers.ts` הוא זרע היסטורי בלבד? (משפיע על DUP-1) | החלטת משתמש |
| 3 | אימות סופי ב-grep מקומי ש-`transformResearchOutputToDBRecord` חסר קורא | grep על המאגר |
