# הרהור: זהות ישויות בשער הכניסה — האם הארכיטקטורה נכונה?

מסגרת הכיול שנקבעה: קבוצה קטנה, מקרה בוחן יחיד, הוכחת יכולת, ללא סקייל.
"מספיק חזקה, לא הכי חזקה". לא כל פגם ראוי לטיפול. ההרהור נעשה בתוך
המסגרת הזו, לא למרות שהיא.

---

## 1. הארכאולוגיה — מה באמת קרה

לא "חסר אילוץ ולכן נכנסו כפילויות". הסיפור מדויק יותר:

- **למערכת יש מושג זהות, והוא עובד**: ה-id של ארגון הוא slug אנושי
  (`ccuk`, `afa`, `ccfi-israel`), וזריעה עושה `upsert(onConflict: "id")`.
  זריעה חוזרת עם אותם slugs לא מכפילה כלום.
- הכפילויות נוצרו כי **שני דורות של נתוני זריעה** (שניהם 2025-12-30)
  קראו לאותם ארגונים אמיתיים ב-slugs שונים: `ccuk` לעומת `ccuk-uk`,
  `ccfa` לעומת `ccf-usa`. השער עשה בדיוק מה שנאמר לו. **המפתח** היה
  לא-עקבי, לא המנגנון.
- וזה גרוע מכפילות רגילה: השוואת שדות מראה ש**כל תאום מחזיק חצי אחר
  של האמת**:

| שדה | `ccuk` (יתום) | `ccuk-uk` (מוצבע ע"י GBR) |
|---|---|---|
| אנשי קשר | Sarah Sleet, CEO + ✉ | ריק |
| אתר, שנת ייסוד | ✓ 1979 | ריק |
| ניתוח אסטרטגי | מלא | ריק |
| שם עברי | — | ✓ |
| ציוני תעדוף | 5/5 בסיסי | 9/8 מעודכן |
| חברים | 80,000 | 500,000 |

  ואצל הזוג השני, `ccf-usa` (השורה ה"עדכנית") מכיל
  `strategic_analysis = {"intelligence": "Test intel"}` — **נתון בדיקה
  בייצור** — ו-5,000,000 חברים לעומת 150,000 אצל התאום.

- טווח הפגיעה מוגבל אך לא אפס: 0 דוחות, 0 הערות, 0 העשרות על כל
  הארבעה; הפניה חיה אחת — `country_strategies['GBR'].key_organization_ids`
  מצביע על `ccuk-uk`.

**מסקנת הארכאולוגיה:** מחיקה עיוורת של כל אחד מהצדדים מאבדת מידע אמיתי.
זו לא בעיית constraint קודם כל — זו בעיית מיזוג, ואחריה constraint שימנע
את הפעם הבאה.

## 2. למה דווקא הפגם הזה עובר את רף ה"שווה לתקן"

הכיול אומר: פגם שמכאיב בקול או בסקייל — נחכה. הזהות שונה משאר הפגמים
שחנינו בצד בשלושה מאפיינים שנמצאים יחד רק אצלה:

1. **שקטה.** אינדקס חסר מאט — מרגישים. Verdict לא מחושב — רואים NULL.
   ישות כפולה לא מתריעה על כלום; הדשבורד פשוט מציג שני ארגונים.
2. **מצטברת.** כל דוח מודיעין עתידי ייתפס באחד התאומים לפי בחירת
   המשתמש באותו רגע. הראיות מתפצלות, ושתי השורות משקרות באותה מידה.
   היום המיזוג הוא UPDATE אחד; בעוד עשרים דוחות הוא עבודה פורנזית.
3. **בליבת הערך.** המוצר הזה הוא "תגיד לי הכול על X". פיצול X הוא
   כשל של המוצר עצמו, לא של תשתיתו. במאזן "מספיק חזקה" — שלמות
   הזהות היא מהדברים שחייבים להיות חזקים, כי בלעדיה ההוכחת-יכולת
   מוכיחה את ההפך.

עלות מניעה: אינדקס אחד + התנהגות שגיאה בזריעה. נזק נמנע: הצטברות
שקטה. היחס הזה הוא הסיבה שזה בפנים כשה-FK indexes בחוץ.

## 3. האם הארכיטקטורה נכונה להקשר? — כן, עם טלאי אחד

**מה שנכון ושווה לשמור:**
- **slug אנושי כזהות** הוא בחירה טובה לסקייל הזה: קריא, ניתן לאזכור
  במערכי related_*, ידידותי ל-upsert. עדיף גם על uuid בלבד (אין ידית
  טבעית לדה-דופ) וגם על שם-כזהות (לשמות יש וריאנטים; slug הוא הכרעה).
- **שער כניסה יחיד לארגונים.** אין שום insert לארגונים מהקליינט —
  רק זריעת אדמין. שטח שצריך לשמור עליו: נקודה אחת.
- **הפניות פולימורפיות** (entity_id+entity_type בלי FK) ומערכי
  related_* בלי FK — ויתור מודע על שלמות הצהרתית תמורת פשטות סכמה.
  במערכת 4 משתמשים זה ויתור סביר; מחירו נגבה רק במיזוגים, ומדדנו
  אותו: הפניה חיה אחת.

**הפער היחיד:** אין שום דבר שמכריח ששני slugs שונים לא יתארו את אותו
ארגון אמיתי. את הבעיה הזו במלואה אי אפשר לפתור באילוץ — זו בעיית
entity resolution, ופתרונה המלא (fuzzy matching, טבלת כינויים, ממשק
מיזוג) הוא הנדסת-יתר מובהקת כאן. אבל אפשר להפוך את מחלקת הכשל
**שנצפתה בפועל** — אותו שם תצוגה בדיוק תחת slug אחר — מכשל שקט לשגיאה
רועשת: אינדקס ייחודי על `lower(name)`. וריאנטים אמיתיים ("CCF" מול השם
המלא) נשארים באחריות האוצרות האנושית — וזה בסדר, זו בדיוק החוזקה של
צוות של 4.

**מה לא בונים (הצד השני של המאזן):** התאמה מטושטשת, טבלת aliases,
ממשק מיזוג, ניקוד דמיון, FK על מערכים. נרשם כמגבלה ידועה.

## 4. התוכנית המוצעת — לא בוצע דבר

### א. מיזוג (UPDATE אחד + DELETE אחד לכל זוג)

מנצחים: `ccuk-uk` (מוצבע ע"י GBR) ו-`ccf-usa` (עקבי עם מוסכמת
ה-slug הגיאוגרפית). לתוכם נמזג מהיתומים: contacts, email, website,
founded_year, strategic_analysis, tags (איחוד), country בנוסח המלא.

שתי הכרעות תוכן שרק אתה יכול לקבוע:
1. **member_count**: 80,000 או 500,000 לבריטניה? 150,000 או 5,000,000
   לארה"ב? (הערכים הנמוכים נראים כחברות בפועל; הגבוהים כ"מושפעים".)
2. `ccf-usa` מחזיק `"Test intel"` ותקציב 100,000,000 — לדרוס בנתוני
   היתום או שיש סיבה לערכים האלה?

### ב. תיל-מעידה (מיגרציה קטנה)

```sql
CREATE UNIQUE INDEX IF NOT EXISTS organizations_name_lower_key
  ON public.organizations (lower(trim(name)));
CREATE UNIQUE INDEX IF NOT EXISTS influencers_name_lower_key
  ON public.influencers (lower(trim(name)));
```

(מותנה בכך שהמיזוג קודם — אחרת האינדקס ייכשל על הכפולות הקיימות.
בדקתי: אצל משפיענים אין כפולות היום.)

### ג. שער רועש (שינוי קוד קטן, לא דחוף)

`seed-influencers-orgs` עושה upsert אחד גדול — התנגשות שם תפיל את כל
האצווה עם שגיאת constraint גולמית. לעבור לשורה-שורה עם איסוף שגיאות
(מבנה ה-errors כבר קיים בקוד) כך שהאדמין יקבל "נדחה: שם קיים תחת
slug אחר" ליד כל שורה בעייתית.

### מה נשאר בחוץ במודע

וריאנטים של שמות, מיזוג אוטומטי, כלי מיזוג ב-UI. וכל שאר רשימת
ה"נמוך" מהביקורת — בעינה.
